# SEO Loop Log

This file is the per-loop track record for the recurring 5-minute SEO/AEO optimization
loop on the Terraza del Pacifico site. Each loop does real keyword research (SE Ranking),
makes ONE small, truthful, high-impact fix to an Experience or Event page, and logs it
here. Truth guardrail: this resort is in **Playa Hermosa de Jacó, Puntarenas — Costa Rica's
central Pacific (surf) coast**, NOT the unrelated Playa Hermosa in Guanacaste. Every
geographic claim is verified against the repo's canonical NAP (`lib/seo.ts`,
`components/home/location.tsx`) before use.

---

### Loop 1 — 2026-06-24 — Events hub (`/eventos`)
- **Keyword data (SE Ranking):** `playa hermosa jaco` 140 vol / KD 11 (source `cr`); `playa hermosa costa rica hotel` 390 vol / KD 10 (source `us`, low vol on `cr`); `beach wedding costa rica` 70 vol / KD 10, intent Local+Commercial (source `us`). The events hub `<title>` had zero geo keyword while every other hub/detail page already targets "Playa Hermosa".
- **Change:** Events hub title in `content/events.ts:12-13` — `es: "Eventos frente al mar"` → `"Eventos frente al mar en Playa Hermosa"`; `en: "Oceanfront events"` → `"Beachfront Events in Playa Hermosa"`. This field feeds the metadata `<title>`, OpenGraph title, breadcrumb, and on-page H1 for both locales (`app/[locale]/eventos/page.tsx`).
- **Truth check:** "Playa Hermosa" verified against canonical NAP in `lib/seo.ts` (Playa Hermosa, Jacó, Puntarenas) and `components/home/location.tsx`; no Guanacaste/Papagayo conflation.
- **Next candidates:** (1) `otherEventsHub` title "Spaces for every occasion" / "Espacios para cada ocasion" — also geo-free, same fix pattern. (2) Surf Nights detail page (`content/events.ts` surf-nights) — strong topical "night surfing" angle but could test surf-intent geo keywords.

### Loop 2 — 2026-06-24 — Weddings detail (`/events/bodas`)
- **Keyword data (SE Ranking):** `cr` database empty for all wedding seeds → used `us`. `destination wedding costa rica` 480 vol / KD 17 (intent Informational); `beach wedding costa rica` 70 vol / KD 10 (intent **Local + Commercial** — best fit for a venue page); `costa rica wedding venue` 10 / KD 13 (L+C). The weddings detail `<title>` was the strongest commercial money page yet had ZERO geo in its title tag (it only carried geo in the on-page eyebrow, which search engines weight far below `<title>`).
- **Change:** Weddings title in `content/events.ts:84-87` — `es: "Bodas frente al mar"` → `"Bodas frente al mar en Playa Hermosa, Costa Rica"`; `en: "Beachfront Weddings"` → `"Beachfront Weddings in Playa Hermosa, Costa Rica"` (~48 chars). This field feeds the metadata `<title>`, OpenGraph/Twitter title, breadcrumb, and on-page H1 for both locales (`app/[locale]/events/[slug]/page.tsx` → `page.title[l]`). Now targets "beach wedding costa rica" (L+C) + geo.
- **Truth check:** "Playa Hermosa, Costa Rica" verified against canonical NAP in `lib/seo.ts` (Playa Hermosa, Jacó, Puntarenas, CR); no Guanacaste/Papagayo conflation. Resort genuinely hosts weddings (amenityFeature "Event & wedding venue" + dedicated wedding venues described in page body).
- **Next candidates:** (1) `otherEventsHub` title "Spaces for every occasion"/"Espacios para cada ocasion" — still geo-free (geo only in its description). (2) Corporate-events or private-dinners detail `<title>` — both geo-free in the title tag, geo only in description.

### Slug migration — 2026-06-24 — site-wide (Pass 1: parent segments)
- **Change:** Renamed all 9 Spanish parent route segments to English (experiencias→experiences, eventos→events, habitaciones→suites, bares→bars, galeria→gallery, restaurante→restaurant, panaderia→bakery, politicas→policies, sobre-nosotros→about). Children kept (e.g. /events/bodas, /suites/estandar). 39 files updated via path-bounded replace (Spanish prose preserved).
- **Redirects:** 308 permanent old→new for both locales incl. nested wildcards (next.config.mjs). Verified live: /en/eventos→/en/events, /en/habitaciones/estandar→/en/suites/estandar.
- **Verified:** prod build OK; page loads 200 (en+es); sitemap emits English URLs only. Reason: most traffic is English; aligns URLs to audience + PostHog tracks final URLs from day one.
- **Pass 2 (deferred):** child slugs still Spanish — bodas→weddings, otros→spaces, estandar→standard, comparar→compare — pending read of each child page's content-resolution before rename.

### Loop 3 — 2026-06-24 — Surf Nights detail (`/events/surf-nights`)
- **Keyword data (SE Ranking):** `cr` database empty for all surf seeds (only `playa hermosa jaco` 140 vol / KD 11 exists) → used `us`. `surfing playa hermosa` 390 vol / KD 15 (intent Informational); `playa hermosa surf` 390 vol / KD 19 (intent **Local + Commercial** — best fit for a venue/event page); `surf jaco costa rica` / `jaco surf` 10 vol / KD 10 (L+C). The Surf Nights detail `<title>` was a bare brand name ("Surf Nights") — the only Experience/Event detail title with ZERO geo and ZERO descriptive keyword, despite surf being Playa Hermosa de Jacó's signature draw.
- **Change:** Surf Nights title in `content/events.ts:271-274` — `es/en: "Surf Nights"` → `es: "Surf Nights en Playa Hermosa, Costa Rica"`, `en: "Surf Nights in Playa Hermosa, Costa Rica"` (~40 chars). This field feeds the metadata `<title>`, OpenGraph title, breadcrumb leaf, and on-page H1 (`app/[locale]/events/[slug]/page.tsx` → `page.title[l]`). "Surf Nights" brand kept English in both locales (matches existing description usage). Now puts `surf` (from brand) + `playa hermosa` adjacency in the title to catch the 390-vol `playa hermosa surf` (L+C). Verified the `Ready to plan {title}?` CTA interpolation (event-info-page.tsx:208) still reads grammatically in both locales. Eyebrow + description already carry geo — left as-is.
- **Truth check:** "Playa Hermosa, Costa Rica" verified against canonical NAP in `lib/seo.ts` (Playa Hermosa, Jacó, Puntarenas, CR). "surf" actively DISAMBIGUATES toward the correct Playa Hermosa — the Jacó beach is the famous surf beach, the Guanacaste one is calm water — so the surf keyword strengthens rather than risks geo conflation. Event is real (live televised night-surf competition described in page body, "one of only 4 places in the world" with night-surf infrastructure).
- **Next candidates:** (1) `otherEventsHub` title "Spaces for every occasion"/"Espacios para cada ocasion" — still geo-free in the title tag (geo only in description). (2) private-dinners or family-celebrations detail `<title>` (within otros hub) — geo-free in title.

### Loop 4 — 2026-06-24 — Other Events hub (`/events/other-events`)
- **Keyword data (SE Ranking):** `cr` database empty for all event-venue seeds → used `us`. `costa rica events` 170 vol / KD 6 (intent Informational); `playa hermosa jaco` 110 vol / KD 9 (I); `corporate retreat costa rica` 30 vol / KD 3; `costa rica wedding venue` 10 vol / KD 13 (**Local + Commercial**). `event venue` itself is 110k/KD 70 (too broad/competitive). The `otherEventsHub` `<title>` ("Spaces for every occasion") was the last Event-section hub/detail title with ZERO geo — geo lived only in its description, which `<title>` outweighs. Flagged as the next candidate in Loops 1, 2, and 3.
- **Change:** `otherEventsHub` title in `content/events.ts:41-42` — `es: "Espacios para cada ocasion"` → `"Espacios para eventos en Playa Hermosa, Costa Rica"`; `en: "Spaces for every occasion"` → `"Event Spaces in Playa Hermosa, Costa Rica"` (~41 chars EN). This field feeds the metadata `<title>`, OpenGraph title, breadcrumb leaf, and on-page H1 for the other-events hub in both locales (`app/[locale]/events/[slug]/page.tsx:86,97,138`). Now carries "Event Spaces" + "Playa Hermosa" + "Costa Rica" geo/intent. Verified this hub has NO `Ready to plan {title}?` CTA interpolation (that pattern is per-event on detail pages, not the hub) — safe.
- **Truth check:** "Playa Hermosa, Costa Rica" verified against canonical NAP in `lib/seo.ts` (Playa Hermosa, Jacó, Puntarenas, CR); no Guanacaste/Papagayo conflation. Hub genuinely hosts corporate gatherings, family celebrations, private dinners and parties (description + 4 detail pages: corporate-events, family-celebrations, private-dinners, parties-and-celebrations).
- **Next candidates:** (1) Experience detail META DESCRIPTIONS — whale-watching/beachfront-yoga/turtle-nesting/live-music titles still geo-free in the title tag (geo only in eyebrow/description); whale-watching title "Whale Watching" is the strongest untouched detail `<title>`. (2) Experience hub description is fine; consider the individual experience `<title>` tags next.

#### Fix — 2026-06-24 — broken weddings link (post-migration)
- **Bug:** EN homepage weddings card 404’d at /en/events/bodas. Root cause: pre-existing latent mismatch — lib/dictionaries.ts EN block hardcoded the SPANISH detail slug (events/bodas) used for both locales; event details are served by events/[slug] with localized slugs (es:bodas, en:weddings). The parent rename surfaced it.
- **Fix:** lib/dictionaries.ts:119 slug "events/bodas" → "events/weddings" (EN block only; ES block events/bodas is correct). Verified: /en homepage 200, card href now /en/events/weddings (200). All other homepage cards already 200 in both locales.
- **Note:** A separate 500 was a corrupted .next from multiple competing dev servers — reset to a single clean dev server (NOT a code issue).

### Loop 5 — 2026-06-24 — Whale Watching detail (`/experiences/whale-watching`)
- **Keyword data (SE Ranking):** EN (`us`): `whale watching costa rica` / `costa rica whale watching` 10 vol / KD 17, intent **Local + Commercial** (best venue fit); `playa hermosa jaco` 110 vol / KD 9 (I); `whale watching jaco` / `whale watching central pacific costa rica` no data. ES (`cr`): `avistamiento de ballenas costa rica` **260 vol / KD 6, intent L+C** (strong, low difficulty); `whale watching uvita` 10 / KD 16. The whale-watching detail `<title>` was a bare descriptor ("Whale Watching" / "Avistamiento de Ballenas") with ZERO geo — geo lived only in eyebrow/description, which `<title>` outweighs. First Experience-section detail title to be geo-tagged (Loops 1–4 covered all Event titles).
- **Change:** Whale Watching title in `content/experiences.ts:42-43` — `en: "Whale Watching"` → `"Whale Watching in Costa Rica - Playa Hermosa, Jaco"` (~50 chars); `es: "Avistamiento de Ballenas"` → `"Avistamiento de Ballenas en Costa Rica - Playa Hermosa"` (~54 chars). Feeds metadata `<title>`, OpenGraph title, breadcrumb leaf, and on-page H1 for both locales (`app/[locale]/experiences/[slug]/page.tsx:27,38,65` → `page.title[l]`). Targets `whale watching costa rica` (EN L+C) + `avistamiento de ballenas costa rica` (ES 260-vol L+C) while anchoring the truthful base location. NO `Ready to plan {title}?` CTA interpolation in the experience template (that's the events template) — title is used only as H1/breadcrumb/form `pageTitle`; reads fine. Typecheck clean.
- **Truth check (geography verified for nature activity):** Whale watching is a REGIONAL Central Pacific Costa Rica offering, NOT an "at Playa Hermosa" claim. The page body is careful: humpbacks "arrive in the warm waters of Costa Rica's Pacific" and Playa Hermosa (minutes south of Jacó) is "a natural starting point" / base, not the sighting spot. Title therefore leads with "Costa Rica" (the true regional scope) and keeps "Playa Hermosa, Jacó" as the resort base — no overclaim. All geo verified against `lib/seo.ts` NAP (Playa Hermosa, Jacó, Puntarenas, CR); central-Pacific Jacó NOT conflated with the Guanacaste/Papagayo Playa Hermosa.
- **Next candidates:** (1) `turtle-nesting` detail `<title>` "Turtle Nesting"/"Anidacion de Tortugas" — geo-free, same pattern; verify it's a regional/nearby activity (body says Playa Hermosa is a "natural setting" for conservation — likely a base claim, not a guaranteed-on-site nesting beach, so geo-tag as Central Pacific / near Jacó). (2) `beachfront-yoga` or `live-music` detail `<title>` — both geo-free; these ARE genuinely on-site at the resort so "Playa Hermosa" is safe in title.

### Loop 6 — 2026-06-24 — Beachfront Yoga detail (`/experiences/beachfront-yoga`)
- **Keyword data (SE Ranking):** EN (`us`): `beach yoga` **4400 vol / KD 37, intent L+C** (strong); `yoga costa rica` 390 vol / KD 34 (L+C); `beachfront yoga` 70 vol / KD 17 (L+C, EXACT match — low difficulty); `yoga retreat costa rica` 40 / KD 36 (I). ES (`cr`): `yoga costa rica` **170 vol / KD 10, intent L+C** (low difficulty); `yoga frente al mar` / `yoga jaco` / `yoga en la playa` no data. Compared `live-music` seeds — far weaker (`live music costa rica` only 20 vol / KD 6), so yoga is the higher-value target. The beachfront-yoga detail `<title>` was a bare descriptor ("Beachfront Yoga" / "Yoga Frente al Mar") with ZERO geo — geo lived only in eyebrow/description, which `<title>` outweighs. Flagged as a safe next candidate in Loop 5.
- **Change:** Beachfront Yoga title in `content/experiences.ts:335-338` — `en: "Beachfront Yoga"` → `"Beachfront Yoga in Playa Hermosa, Costa Rica"` (~44 chars); `es: "Yoga Frente al Mar"` → `"Yoga Frente al Mar en Playa Hermosa, Costa Rica"` (~48 chars). Feeds metadata `<title>`, OpenGraph/Twitter title, breadcrumb leaf, and on-page H1 for both locales (`app/[locale]/experiences/[slug]/page.tsx` → `InfoDetailTemplate`, `detail-template.tsx:130` H1 + `:375/:404` form `pageTitle`). Targets `beach yoga` / `beachfront yoga` (EN L+C) + `yoga costa rica` (170-vol ES L+C). Verified: detail route uses `InfoDetailTemplate` (no `Ready for {title}?` CTA — that interpolation lives in the unused `info-page.tsx:202`, where the title still reads grammatically: "Ready for Beachfront Yoga in Playa Hermosa, Costa Rica?"). Typecheck clean.
- **Truth check (geography verified for on-site activity):** Beachfront yoga is GENUINELY on-site — it's a listed hotel amenity in `lib/seo.ts` ("Beachfront yoga") with a fixed published schedule in the page body (Sun/Wed 8 AM, Sat 4 PM, "For guests"). So "in Playa Hermosa" is a true on-site claim, not a regional approximation. All geo verified against `lib/seo.ts` NAP (Playa Hermosa, Jacó, Puntarenas, CR); central-Pacific Jacó NOT conflated with the Guanacaste/Papagayo Playa Hermosa.
- **Next candidates:** (1) `live-music` detail `<title>` "Live Music"/"Musica en Vivo" — geo-free, genuinely on-site (Vivace Beachfront, weekends) so "Playa Hermosa" safe; but low keyword volume, so weave geo without overinvesting. (2) `turtle-nesting` detail `<title>` — geo-free; frame regionally (Central Pacific / near Jacó), NOT an on-site nesting claim.

### Loop 7 — 2026-06-24 — Turtle Nesting detail (`/experiences/turtle-nesting`)
- **Keyword data (SE Ranking):** EN (`us`): `sea turtle costa rica` **480 vol / KD 13** (intent I); `turtle nesting costa rica` 70 vol / KD 10 (I); `turtle nesting jaco`/`turtle nesting playa hermosa` no data. ES (`cr`): `tortuga marina` **480 vol / KD 18** (I); `playa hermosa jaco` 140 vol / KD 11 (I); `anidacion de tortugas`/`tortugas costa rica`/`desove`/`liberacion` all no data. Compared `live-music` (the other remaining geo-free experience title): far weaker — `live music costa rica` only 20 vol / KD 6. Turtle nesting is the clearly higher-value target, so chose it over live-music this loop. The turtle-nesting detail `<title>` was a bare descriptor ("Turtle Nesting" / "Anidacion de Tortugas") with ZERO geo — geo lived only in eyebrow/description, which `<title>` outweighs.
- **Change:** Turtle Nesting title in `content/experiences.ts:188-191` — `en: "Turtle Nesting"` → `"Sea Turtle Nesting in Costa Rica - Playa Hermosa, Jaco"` (~53 chars); `es: "Anidacion de Tortugas"` → `"Anidacion de Tortugas en Costa Rica - Playa Hermosa, Jaco"` (~56 chars). Feeds metadata `<title>`, OpenGraph/Twitter title, breadcrumb leaf, and on-page H1 for both locales (`app/[locale]/experiences/[slug]/page.tsx` → `InfoDetailTemplate`). Added "Sea" to the EN title to capture the 480-vol `sea turtle costa rica` head term (the strongest seed); ES keeps "tortuga marina" adjacency via "Anidacion de Tortugas ... Costa Rica". Detail route uses `InfoDetailTemplate` (no `Ready for {title}?` CTA interpolation). Typecheck clean.
- **Truth check (geography verified for nature activity):** Lead with "Costa Rica" (true regional scope), then "Playa Hermosa, Jaco" as the resort base — matching the whale-watching (Loop 5) precedent. Unlike whale-watching, the body DOES frame Playa Hermosa as the on-site setting ("Playa Hermosa, just south of Jaco, is a natural beachfront setting where sea turtles are part of a story of conservation"; references "the Playa Hermosa beach"), and Playa Hermosa de Jacó is genuinely a designated sea-turtle nesting refuge — so "Playa Hermosa" is truthful, not an overclaim. The body still hedges availability ("Seasonal and variable", "when activities are available"), so the title deliberately does NOT promise guaranteed nesting; it names the activity + true location only. All geo verified against `lib/seo.ts` NAP (Playa Hermosa, Jacó, Puntarenas, CR); central-Pacific Jacó NOT conflated with the Guanacaste/Papagayo Playa Hermosa.
- **Next candidates:** Geo-free experience/event TITLES are now nearly exhausted — only `live-music` remains (genuinely on-site, but low volume: `live music costa rica` 20 vol). Next leverage shifts to META DESCRIPTIONS / H2 intros: e.g. weave the 480-vol `sea turtle costa rica` and `tortuga marina` into the turtle-nesting description, or geo-enrich the experiences-hub intro. The single remaining title fix (live-music) is low-priority by volume.

### Loop 8 — 2026-06-24 — Whale Watching detail (`/experiences/whale-watching`) — META DESCRIPTION
- **Keyword data (SE Ranking):** ES (`cr`): **`avistamiento de ballenas costa rica` 260 vol / KD 6, intent L+C** — standout low-difficulty commercial term; `avistamiento de ballenas` 90 / KD 20 (L+C); `ballenas jorobadas costa rica` 70 / KD 6 (I). EN (`us`): `whale watching costa rica` / `costa rica whale watching` 10 vol / KD 17 (L+C); `humpback whales costa rica` 70 / KD 12 (I); `whale watching jaco` no data. The ES meta description did NOT contain the exact 260-vol phrase (it split it as "ballenas jorobadas... Pacifico costarricense"), and both descriptions were over-length (ES ~205, EN ~181 chars).
- **Change:** `content/experiences.ts:50-51` (whale-watching `description`, which feeds BOTH the Next metadata `description` via `experiences/[slug]/page.tsx:28` AND the on-page intro at `detail-template.tsx:133` — so it was rewritten to read as natural prose). ES → "Avistamiento de ballenas en Costa Rica: cada temporada las jorobadas llegan al Pacifico. Playa Hermosa, al sur de Jaco, es tu punto de partida frente al mar." (157 chars; exact 260-vol phrase now contiguous, leads the snippet). EN → "Whale watching in Costa Rica: each season humpbacks arrive in the warm Pacific. Beachfront Playa Hermosa, south of Jaco, is your starting point to see them." (156 chars). Both tightened from ~200 to ~156. Typecheck clean.
- **Truth check:** Keyword "...costa rica" matches the honest REGIONAL framing established in Loop 5 — whale watching is a Central Pacific Costa Rica offering; Playa Hermosa/Jaco kept as the "punto de partida / starting point" (base), NOT an on-site sighting overclaim. Both geo anchors (Playa Hermosa + Jaco) preserved. Geo verified vs `lib/seo.ts` NAP; no Guanacaste/Papagayo conflation.
- **Next candidates:** (1) Turtle-nesting description — weave the higher-RAW-volume `tortuga marina` / `sea turtle costa rica` (~480 vol each, but Informational, KD 13-18). (2) Weddings description is already keyword-rich; experiences-hub intro could be tightened. (3) Last remaining geo-free title: `live-music` (low volume).

### Loop 9 — 2026-06-24 — Turtle Nesting detail (`/experiences/turtle-nesting`) — META DESCRIPTION
- **Keyword data (SE Ranking):** ES (`cr`): **`tortuga marina` 480 vol / KD 18 (I)**; **`desove de tortugas` 140 vol / KD 14 (I)** — the real ES term for nesting (note: title's `anidacion de tortugas` returns NO data); `playa hermosa jaco` 140 / KD 11. EN (`us`): **`sea turtles costa rica` 480 / KD 11**, `costa rica sea turtles` 480 / KD 10, `sea turtle costa rica` 480 / KD 13 (all I); `turtle hatching costa rica` 210 / KD 8; `turtle nesting costa rica` 70 / KD 10. Old description led with "Playa Hermosa..." and buried `tortugas marinas` mid-sentence (never the clean 480-vol `tortuga marina` phrase), and was over-length (ES ~184).
- **Change:** `content/experiences.ts:197-198` (turtle-nesting `description` — feeds BOTH the Next metadata `description`/OpenGraph via `experiences/[slug]/page.tsx:28,39` AND the on-page intro at `detail-template.tsx`, so written as natural prose). ES → "Tortuga marina y desove en Playa Hermosa, al sur de Jaco: un entorno frente al mar dedicado a la conservacion, la educacion y el respeto por la vida silvestre." (~157 chars; leads with 480-vol `tortuga marina` + earns the 140-vol `desove de tortugas` the title misses). EN → "Sea turtles in Costa Rica: Playa Hermosa, just south of Jaco, is a beachfront base for turtle nesting season, conservation and respect for wildlife." (~146 chars; leads with `sea turtles ... costa rica` + keeps `turtle nesting`). ES trimmed from ~184 to ~157. Typecheck clean.
- **Truth check:** Hedge preserved — framed as a conservation/"entorno...dedicado a la conservacion" + "beachfront base for turtle nesting season", NOT a guaranteed-sighting claim ("ven a ver tortugas" avoided). Matches the Loop 7 body framing (Playa Hermosa de Jaco IS a real sea-turtle nesting refuge, but body hedges "Seasonal and variable"). Geo verified vs `lib/seo.ts` NAP (Playa Hermosa, Jaco, Puntarenas, CR); no Guanacaste/Papagayo conflation.
- **Next candidates:** (1) Weddings description (already keyword-rich — light tightening only). (2) Experiences/events HUB descriptions — geo-enrich the intro snippets. (3) Last geo-free title `live-music` (low volume, ~20).

### Loop 10 (NEW SERP-aware mode demo) — 2026-06-24 — Weddings
- **Target keyword(s) + SERP:** Live organic SERP via SE Ranking `DATA_getSerpResults` for `costa rica wedding venue` (CR-targeted query timed out twice; ran **US** DB instead — note DB). TOP 5 ranking URLs: 1) weddingwire.com "The 10 Best Wedding Venues in Costa Rica" 2) reddit.com r/DestinationWeddings 3) theknot.com "Costa Rica Destination Wedding — Venues, Tips & Marriage…" 4) tripadvisor.com "THE 10 BEST Wedding Resorts in Costa Rica" 5) planethollywoodhotels.com "Dream Weddings in Costa Rica" (also fourseasons.com, destinationweddings.com). Real questions (`DATA_getKeywordQuestions`, `costa rica wedding`, us): "how much does a wedding in costa rica cost" (10 vol, KD 11), "how to plan a destination wedding in costa rica" (10, KD 9), "what to wear to a wedding in costa rica", "how much is a destination wedding in costa rica".
- **Content gap found:** Every top ranker leads with a consolidated **"what's included / what does the resort provide"** answer (packages, on-site catering, ceremony+reception spaces, on-site lodging for the wedding party, who plans it) framed for out-of-town destination-wedding searchers. Our page had the facts scattered across prose sections (Guest experience, Gastronomia, Planning inquiry) but no single direct-answer block — weak for AI Overviews / featured-snippet capture.
- **Change (content):** `content/events.ts` — added a new net-new section **"What a wedding here includes" / "Que incluye una boda aqui"** (inserted after Overview, before "Our venues", ~line 186) as a crisp consolidated direct-answer: single beachfront location on Playa Hermosa south of Jaco; four named venues (beachfront garden, evening ceremony, Vivace Terrace, starlit pool); on-site Vivace/Chef Luigi catering; hotel rooms & suites to host guests; on-site events team. Both locales, each field in its own language.
- **Truth check:** Every claim verified in-repo — four venues from the existing "Our venues" section; Vivace + Chef Luigi from "Gastronomia"/restaurant content; on-site rooms/suites from `lib/seo.ts` (suites: standard/superior/junior-suite/villas); events team from "Planning inquiry" + `mercadeo1@`; geo = Playa Hermosa de Jacó, Puntarenas (NOT Guanacaste). NO invented packages, price, or capacity.
- **NEEDS CLIENT INPUT:** (1) Max guest capacity per venue (ceremony & reception) — top rankers all state this; we cannot. (2) Wedding package pricing / "starting from" (highest-intent query "how much does a wedding cost" — guardrail blocks invention). (3) Legal vs symbolic ceremony support / officiant & marriage-license logistics. (4) Whether a guest room block / group rate exists.
- **Next candidates:** (1) Corporate Events detail — add a "What's included" answer block (same gap, capacity NEEDS CLIENT INPUT). (2) Events hub — geo + intent-enrich intro for "event spaces playa hermosa / jaco".

### Loop 11 (SERP-aware) — 2026-06-24 — Surf Nights (`/events/surf-nights`)
- **Target keyword + SERP (region):** `playa hermosa surf` 390 vol / KD 19, intent **L+C** (live SERP via `DATA_getSerpResults`, **US** DB — CR/`surfing playa hermosa` seeds timed out repeatedly; questions endpoint returned empty/429 for all surf seeds). Supporting metrics (us): `surfing playa hermosa` 390/KD15 (I), `best surf season costa rica` 110/KD7 (I), `playa hermosa jaco` 110/KD9 (I), `surf jaco costa rica`/`jaco surf` 10/KD10 (L+C). SERP features present: **ai_overview + people_also_ask** (strong FAQ/AEO target). TOP 6 organic: 1) mytanfeet.com "Playa Hermosa (Jaco): The Surfing Stadium of Costa Rica" 2) surfline.com "Playa Hermosa North Surf Report & Forecast" (swell/season) 3) reddit.com r/surfing "Surf trip in March" (swell timing) 4) bodhisurfyoga.com (intermediate/experienced wave; disambiguates the two Playa Hermosas) 5) jetsetjansen.com "A Quiet Surf Town" 6) thesurfatlas.com "Ultimate Guide ... official World Surfing Reserve".
- **Content gap found:** Every top ranker answers surf-area questions our page never did — is the break beginner or advanced, best season/swell, what the place actually is, and (event-specific) whether spectators can attend. The page had rich event prose but no direct-answer FAQ block and no FAQPage JSON-LD, weak for the live ai_overview/PAA on this SERP.
- **Change (content):** Added a 4-item `faq?` to the surf-nights entry in `content/events.ts` (inserted before `cta`, ~line 490). Renders on-page (`event-info-page.tsx:164`) AND emits FAQPage JSON-LD (`app/[locale]/events/[slug]/page.tsx:174-179`). Q&A (both locales, accent-free to match this entry's convention): (1) "What is Surf Nights?" / "Que es Surf Nights?" — repo-sourced definitional (AI-Overview bait). (2) "Can spectators attend?" / "Pueden asistir espectadores?" — repo-sourced (Spectator experience section). (3) "Is Playa Hermosa good for beginners?" / "...apta para principiantes?" — powerful beach break, World Surfing Reserve, better for experienced surfers; explicitly disambiguates the calmer Guanacaste Playa Hermosa. (4) "When is the best season to surf?" / "Cual es la mejor epoca?" — consistent year-round, largest swell ~May–Nov wet season; routes to WhatsApp for next edition (covers 110-vol `best surf season costa rica`).
- **Truth check:** Q1/Q2 verified verbatim from the entry's own Overview + Spectator experience sections. Q3 "World Surfing Reserve" + "beach break, better for experienced surfers" verified via mytanfeet (top ranker: "first World Surfing Reserve in Central America in 2020", "not suitable for first timers/fresh beginners") — kept the GENERIC "a World Surfing Reserve" wording (not the single-sourced "first/2020" specifics). Guanacaste disambiguation = true and protective. Q4 season = defensible Central-Pacific geographic truth, phrased conservatively ("roughly May to November", "typically"). Geo verified vs `lib/seo.ts` NAP (Playa Hermosa, Jacó, Puntarenas). NO resort-service overclaim (no lessons/rentals), no invented capacity/price; hazard/rip-current framing deliberately omitted as off-purpose.
- **NEEDS CLIENT INPUT:** (1) Does the resort/area offer surf lessons or board rental for guests (high-intent, deliberately omitted)? (2) Confirmed cadence/next date of Surf Nights (currently "announced by date"). (3) Official entry/spectator cost, if any.
- **Next candidate:** Corporate Events detail — add a "What's included" FAQ/answer block (capacity = NEEDS CLIENT INPUT); or geo+intent-enrich the Events hub intro for "event spaces playa hermosa / jaco".

### Loop 12 (SERP-aware) — 2026-06-24 — Corporate Events (`/events/corporate-events`)
- **Target keyword + SERP (region):** `corporate retreat costa rica` 30 vol / KD 3, intent I (live SERP via `DATA_getSerpResults`, **US** DB — note: questions endpoint 429'd "too many requests"). Supporting metrics (us): `team building costa rica` 20 vol / KD 4, intent **L+C**; other corporate-event seeds returned no data. SERP features present: **ai_overview + people_also_ask + related_searches** (strong FAQ/AEO target). TOP 6 organic: 1) villapuntodevista.com "Costa Rica Luxury Corporate Retreats" 2) aguiladeosa.com 3) moanasurfresort.com (Nosara) 4) costaricarios.com "Team Building Retreats & Tour Packages" 5) stayonda.com "Adventure Corporate Retreats ... Remote Teams" 6) vajrajahra.com "Corporate Retreat Venue". PAA: "How much do corporate retreats cost?", "What resort did the Kardashians stay at in Costa Rica?", "What is the 183 day rule in Costa Rica?", "How much does the retreat in Costa Rica cost?".
- **Content gap found:** Top rankers + the AI Overview answer consolidated "what setting / what's provided / what activities / who is it for" questions; our page had rich prose but NO direct-answer FAQ block and NO FAQPage JSON-LD — weak for the live ai_overview/PAA on this SERP. NOTE: the AI Overview is built almost entirely on **Guanacaste** (Tamarindo/Papagayo/Arenal, "minutes from Liberia airport") — a conflation trap; turned it into a disambiguation asset rather than borrowing its facts.
- **Change (content):** Added a 4-item `faq?` to the corporate-events entry in `content/events.ts` (inserted before `cta`, ~line 679). Renders on-page (`event-info-page.tsx:164`) AND emits FAQPage JSON-LD (`app/[locale]/events/[slug]/page.tsx:174-179`). Q&A (both locales, accent-free to match THIS entry's convention): (1) "Can Terraza del Pacifico host corporate retreats?" — repo-sourced (Overview: meetings/retreats/planning/trainings/conferences). (2) "Where is it located for a company retreat?" — Playa Hermosa, minutes south of Jaco, Central Pacific; explicitly disambiguates the Guanacaste Playa Hermosa. (3) "Is food and beverage available for groups?" — Vivace catering/coffee breaks/lunches/dinners (Food & beverage section). (4) "What team-building activities can be added?" — beach time, beachfront yoga, hotel experiences (real amenities only — NO invented rafting/zipline/ATV list from the AIO).
- **Truth check:** Q1/Q3/Q4 verified verbatim from the entry's own Overview / Food & beverage / Team-building sections; "Beachfront yoga" is a listed amenity in `lib/seo.ts`. Q2 geo (Playa Hermosa, minutes south of Jaco, Central Pacific) verified vs `lib/seo.ts` NAP + the entry's own description; Guanacaste disambiguation = true and protective. Deliberately did NOT borrow the AI Overview's airport-distance framing ("minutes from Liberia"), capacity numbers (up to 50/500), or adventure-activity menu — none are repo facts and the Liberia airport line is false for Jaco. NO invented capacity/price.
- **NEEDS CLIENT INPUT:** (1) Max meeting/retreat capacity (group size) — every ranker states this; we cannot. (2) Corporate retreat / day-rate / package pricing (highest-intent PAA "how much do corporate retreats cost" — guardrail blocks invention). (3) Distance/transfer time from SJO (and whether airport transfers are arranged) — rankers lead with airport proximity; not in repo. (4) Meeting-room specifics (AV, sq footage, # of rooms).
- **Next candidate:** family-celebrations or private-dinners detail — add a truthful FAQ block (same gap, capacity = NEEDS CLIENT INPUT); or geo+intent-enrich the Events hub intro for "event spaces playa hermosa / jaco".

### Loop 13 (SERP-aware) — 2026-06-24 — Private Dinners (`/events/private-dinners`)
- **Target keyword + SERP (region):** `romantic dinner costa rica` (live SERP via `DATA_getSerpResults`, `result_type: advanced`, **US** DB — note: this seed returned no metrics; the metrics anchor is `private chef costa rica` **320 vol / KD 8, intent L+C**). SERP features present: **local_pack + people_also_ask + reviews + video + related_searches** (no AI Overview on this one, but strong PAA/local). TOP organic: 1) nantipa.com "Sea-to-Table Romantic Dinner at Sunset in Santa Teresa" 2) sentidonorterestaurant.com (Las Catalinas) 3) opentable.com "Diners' Choice: Romantic restaurants in Costa Rica" 4) ylangylangbeachresort.com "Romantic Beach Dinner" (candlelit gourmet 3-course sunset dinner, private table) 5) casalamia.cr "Romantic Sunset Dinner" ($180/guest) 6) nayarasprings.com "ROMANTIC PRIVATE DINNER" (private room, own chef) 7) relaischateaux.com "Mis Amores" (private chef, intimate room). PAA: "What does a romantic dinner consist of?", "What is a traditional Costa Rican dinner?", "Is $500 enough for a week in Costa Rica?". Related searches: heavily **Guanacaste** (Las Catalinas, Potrero, Tamarindo, Brasilito) — conflation trap, kept geo natural rather than forcing disambiguation.
- **Content gap found:** Top rankers consolidate a direct answer to "what is it / what's the setting / who cooks it / who's it for / how private." Our private-dinners page had rich prose but NO FAQ block and NO FAQPage JSON-LD — weak for the live PAA/local SERP and for AEO. The recipe-intent PAA hits ("how to make a romantic dinner") were skipped as off-purpose for a venue page.
- **Change (content):** Added a 4-item `faq?` to the private-dinners entry in `content/events.ts` (inserted before `cta`, ~line 1015). Renders on-page (`event-info-page.tsx:164`) AND emits FAQPage JSON-LD (`app/[locale]/events/[slug]/page.tsx:174-179`). Q&A (both locales, accent-free to match THIS entry's convention): (1) "What is a private dinner at Terraza del Pacifico?" — repo-sourced (Overview + Espacios sections: intimate, beachfront Playa Hermosa south of Jaco, terraces/oceanfront/Vivace spaces, sunset/waves). (2) "What food is served?" — Chef Luigi's Mediterranean & Italian cuisine FROM Vivace (Chef Luigi section) — deliberately framed as Vivace's cuisine, NOT a per-table "private chef" service (the keyword's tempting but unsupported claim). (3) "Couples or groups?" — couple/family/small group, decor+timing+menu coordinated (Romantic-or-group section). (4) "How do I reserve?" — based on availability, ask in advance (Menu personalization + facts "Based on availability").
- **Truth check:** Every answer verified verbatim from the entry's own sections (Overview / Chef Luigi and Vivace / Romantic or group dinners / Beachfront and terrace settings / Menu personalization) + facts block. CRITICAL: did NOT claim a "private chef" service despite `private chef costa rica` being the strongest keyword — repo only supports private *dining* with Vivace/Chef Luigi cuisine; the keyword's adjacency (chef + cuisine + private setting) is earned truthfully without the overclaim. No invented price ($150–$180 seen on rankers), no sole-use/private-room claim, no proposal package. Geo verified vs `lib/seo.ts` NAP (Playa Hermosa, Jaco, Puntarenas, Central Pacific); Guanacaste conflation avoided (not borrowed despite dominating related searches). Typecheck clean.
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → new "Private Dinners" section):** (1) Per-person price (rankers publish $150–$180/guest). (2) Whether a truly private/sole-use setup exists (private room or dedicated isolated table) vs. a reserved table in the Vivace restaurant. (3) Any defined proposal/anniversary package (decor, cake, flowers, photography).
- **Next candidate:** family-celebrations detail — last remaining geo-free Event detail with NO FAQ (rich repo content: multi-generation, Vivace menus, beach/pool/garden/restaurant spaces); or parties-and-celebrations detail; or geo+intent-enrich the Events hub intro for "event spaces playa hermosa / jaco".

### Loop 14 (SERP-aware) — 2026-06-24 — Family Celebrations (`/events/family-celebrations`)
- **Target keyword + SERP (region):** `birthday party venue costa rica` (live SERP via `DATA_getSerpResults`, `result_type: advanced`, **US** DB). NOTE: ALL family/birthday/anniversary seeds returned `is_data_found:false` in `DATA_getKeywordsMetrics` (us) and `DATA_getKeywordQuestions` returned 0 — so this loop is justified by the content gap + live SERP/AEO features, NOT a volume number. SERP features present: **local_pack + people_also_ask + related_searches** (no AI Overview). TOP organic: 1) auberge.com/altagracia "Weddings & Special Celebrations in Costa Rica" 2) venuereport.com "Birthday Party in Costa Rica" 3) ineventos.com "Kids Birthday Party Places in San José" 4) fourseasons.com "Costa Rica Event Venues" 5) villapuntodevista.com "Large Event Spaces" 6) stayintamarindo.com "Special-Occasion Vacation Planning". CAUTION: PAA was nightlife/party-town intent ("Where is the best party place in CR?", "Is CR a good place to party?", "Where is the party town?") + one organic "near Hermosa, **Guanacaste**" — both off-purpose/conflation traps; deliberately NOT borrowed.
- **Content gap found:** Top rankers (venue directories + resort event pages) lead with a consolidated direct-answer "can you host X / what spaces / what food / how to plan" block framed for out-of-town searchers. Our family-celebrations page had rich prose (Overview, Occasions, Food, Mixed-age spaces, Beachfront) but NO direct-answer FAQ block and NO FAQPage JSON-LD — weak for the live PAA/local SERP and AEO.
- **Change (content):** Added a 4-item `faq?` to the family-celebrations entry in `content/events.ts` (inserted before `cta`, ~line 868). Renders on-page (`event-info-page.tsx:164`) AND emits FAQPage JSON-LD (`app/[locale]/events/[slug]/page.tsx:174-179`). Q&A (both locales, accent-free to match THIS entry's convention — "Cumpleanos", "Jaco"): (1) "Can you host a birthday, anniversary or family reunion?" — repo-sourced (Overview + Occasions); geo (Playa Hermosa, south of Jaco, Central Pacific) woven here. (2) "What spaces work for groups with kids and older adults?" — beach/pool/garden/restaurant + the verified **Children's pool** amenity (`lib/seo.ts:129`) + direct beach access as the truthful family differentiator. (3) "Is there food for different ages and preferences?" — Vivace personalized menus (Food and menu options section). (4) "How do we plan and reserve a family celebration?" — team organizes timing/spaces/services, ask in advance (Occasions + facts).
- **Truth check:** Q1/Q3/Q4 verified verbatim from the entry's own Overview / Occasions / Food and menu options sections. Q2 "children's pool" + "direct beach access" verified as listed amenities in `lib/seo.ts` (Children's pool line 129, Beachfront/direct beach access line 121); beach/pool/garden/restaurant from the entry's "Spaces for mixed ages" section. Geo (Playa Hermosa, south of Jaco, Central Pacific) verified vs `lib/seo.ts` NAP (Playa Hermosa, Jacó, Puntarenas); deliberately did NOT borrow the nightlife/party-town PAA or the Guanacaste organic — kept the page on its family/multi-generation purpose. NO invented capacity, price, package, or decoration/coordination service. Typecheck clean.
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md`):** (1) Max group/guest capacity for a family celebration — rankers/directories state this; we cannot. (2) Whether a celebration package or price exists (birthday/anniversary). (3) Whether decorations, cake, or dedicated event coordination are provided/arranged on-site.
- **Next candidate:** parties-and-celebrations detail — last remaining Event detail with NO FAQ (same gap); or geo+intent-enrich the Events hub intro for "event spaces playa hermosa / jaco"; or move to Experiences hub / live-music detail.

### Loop 15 (SERP-aware) — 2026-06-24 — Parties and Celebrations (`/events/parties-and-celebrations`)
- **Target keyword + SERP (region):** Anchored on **`private party venue` 470 vol / KD 23, intent L+C** (matches what this page IS — a venue for social parties). Live SERP via `DATA_getSerpResults` (`advanced`, **US** DB) for `private event venue costa rica`. Deliberately did NOT anchor on `bachelorette party costa rica` (210 vol / KD 4) — it's intent **I** (itinerary/nightlife searchers, not a venue), and the repo only supports "despedidas/sendoffs" (farewell parties), not a bachelorette/party-planning service. SERP features: **local_pack + people_also_ask + reviews + video + related_searches** (no AI Overview). "People also search for": "Small private event venue costa rica", "Private event venue costa rica **price**", "...**all inclusive**". TOP organic: 2) bluemist.cr "Wedding & Private Event" 3) facebook "private wedding venues for 50-70 guests" 4) weddingwire "10 Best Wedding Venues in CR" 5) villapuntodevista "Large Event Spaces" 6) stayintamarindo "Special-Occasion Vacation Planning" 7) auberge.com "Hacienda" 8) venuereport 9) **terrazasflamingo.com / "Terrazas Events Venue Flamingo"** 11) cvent. CAUTION: Terraza Flamingo (Guanacaste, confusingly similar NAME) + Tamarindo rankers make the Central-Pacific/Jaco vs Guanacaste disambiguation more load-bearing here than usual.
- **Content gap found:** This was the LAST Event detail with NO FAQ block and NO FAQPage JSON-LD — weak for the live PAA/local SERP and AEO. Rankers consolidate direct answers to "where is it / what can you host / what's provided / what's the setting"; our page had rich prose (Overview, Social gatherings, Music, Food and cocktails, Beach and pool) but no direct-answer block.
- **Change (content):** Added a 4-item `faq?` to the parties-and-celebrations entry in `content/events.ts` (inserted before `cta`, ~line 1246). Renders on-page (`event-info-page.tsx:164`) AND emits FAQPage JSON-LD (`app/[locale]/events/[slug]/page.tsx:174-179`). Q&A (both locales, accent-free to match THIS entry's convention — "Jaco", "musica"): (1) "Where is the venue for private parties?" — geo + disambiguation (Playa Hermosa, south of Jaco, Central Pacific; explicitly NOT the Guanacaste Playa Hermosa — extra load-bearing given the Terraza Flamingo collision); private-party/social-party venue framing for the 470-vol L+C term. (2) "What kind of parties can be hosted?" — sendoffs, friend gatherings, private celebrations, social events (Social gatherings section, verbatim). (3) "Are music, DJ and catering options available?" — kept CONDITIONAL ("can be coordinated") from Music + Food and cocktails sections — NOT converted to standing services. (4) "Can the party be by the pool or on the beach?" — Beach and pool section.
- **Truth check:** All 4 answers trace to named sections/facts in the entry (Overview, Social gatherings, Music and entertainment, Food and cocktails, Beach and poolside). DJ/live-music/catering kept conditional ("can be coordinated", "can support") per the source copy — no overclaim of a party-planning or vendor service. Geo (Playa Hermosa, south of Jaco, Central Pacific) verified vs `lib/seo.ts` NAP; Guanacaste / Terraza Flamingo conflation explicitly disclaimed. Did NOT borrow the bachelorette/nightlife intent or the price/all-inclusive PAA. NO invented capacity, price, package, or vendor names. Typecheck clean.
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md`):** (1) All-inclusive party package y/n + "starting from" price (the "price"/"all inclusive" People-also-search-for signals). (2) Whether DJ / live-music / decor are arranged via the resort or BYO vendors. (3) Max group/guest capacity for a private party ("Small private event venue" signal).
- **Next candidate:** ALL Event details now have FAQs. ROTATE TO EXPERIENCES — `live-music` is the last experience detail with no FAQ (genuinely on-site at Vivace, low volume), or revisit experiences-hub intro; whale-watching/turtle-nesting/beachfront-yoga already covered via title/description loops (could still get FAQs).

### Loop 16 (SERP-aware) — 2026-06-24 — Beachfront Yoga detail (`/experiences/beachfront-yoga`)
- **Target keyword:** `beach yoga` 4400 vol / KD 37 + `yoga costa rica` 390 EN / 170 ES, intent L+C (from Loop 6). Live SERP region **cr**, lang **en** (`beach yoga costa rica`); questions seed **us** (cr empty for questions).
- **SERP top rankers (cr/en):** bodhitreeyogaresort.com (Nosara), soulspace.website, **Terraza del Pacifico's own Facebook "Yoga Gratis Frente al Mar"** (confirms the on-site offering is real), puravidatraveling.com (Manuel Antonio guide), surfandyogacostarica.com (Santa Teresa), enchanting-costarica.com "6 Reasons Why Beach Yoga in Costa Rica is Best", costaricatreehouse.com "Yoga on the Beach in Costa Rica". Real questions: beginners/levels, schedule, "where to buy a yoga mat in costa rica" (mat-prep signal).
- **Content gap:** First EXPERIENCE detail to get an FAQ (Loops 10–15 covered all EVENT details). Rankers answer practical logistics (beginners? schedule? included? what to bring/mat?) in scannable Q&A; our page had the facts in prose (Schedule, Who can join, What to bring) but NO FAQ block and NO FAQPage JSON-LD.
- **Change (content):** Added a 4-item `faq?` to the beachfront-yoga entry in `content/experiences.ts:469` (inserted before `cta`). Renders on-page (`detail-template.tsx:308`) AND emits FAQPage JSON-LD (`app/[locale]/experiences/[slug]/page.tsx:68-69`). Q&A (both locales, accent-free answers to match THIS entry's convention — "Jaco", "practica", "miercoles"): (1) "Is beachfront yoga suitable for beginners?" — all levels (facts + "Who can join"). (2) "What is the yoga class schedule?" — Sun/Wed 8 AM, Sat 4 PM, may adjust for weather (verbatim from Schedule section, caveat kept). (3) "Who can join the yoga classes?" — for resort guests (entry `Included: For guests` — NOT "free/Gratis", which is an external Facebook claim not in the repo). (4) "What should I bring to the yoga class?" — comfortable clothing, water, arrive early, tell instructor of conditions (What to bring section verbatim; NO mat-provision claim since the entry is silent on mats).
- **Truth check:** All 4 answers trace to the entry's own facts/sections. "in Playa Hermosa, south of Jaco" already in title/description (Loop 6); on-site claim is safe (Beachfront yoga is a listed amenity in `lib/seo.ts` with a published schedule). Did NOT import "free/Gratis" from the FB ranker — used "for resort guests" per the entry's `Included` fact. Did NOT assert mats provided (entry silent). Central-Pacific Jaco NOT conflated with Guanacaste Playa Hermosa. Typecheck clean.
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → new Experiences section):** (1) Are yoga mats provided, or do guests bring their own? (entry's "What to bring" is silent — mat is a top searcher question, "where to buy a yoga mat in costa rica"). (2) Is the beachfront yoga free, and is it open to non-guests/the public? (FB post says "Gratis" + "open" but entry only says "For guests" — can't publish "free/public" without confirmation).
- **Next candidate:** `live-music` is the last experience detail with no FAQ (genuinely on-site at Vivace Beachfront, weekends — low volume but safe). Then whale-watching / turtle-nesting could get FAQs (frame whale-watching/turtle regionally, NOT on-site).

### Loop 17 (SERP-aware) — 2026-06-24 — Whale Watching detail (`/experiences/whale-watching`)
- **Target keyword + SERP (region):** ES `avistamiento de ballenas costa rica` 260 vol / KD 6, intent L+C (the standout from Loops 5/8). Live SERP via `DATA_getSerpResults` (`advanced`) for BOTH **CR** (`avistamiento de ballenas costa rica`) and **US** (`whale watching costa rica`) — both client-polls timed out at 2 min but completed server-side; recovered via `DATA_getSerpTaskAdvancedResults` (CR task 128146591, US task 128150275). SERP features BOTH SERPs: **ai_overview + people_also_ask + reviews + video** (strong FAQ/AEO target). TOP organic — **US**: 1) mytanfeet.com "Costa Rica Whale Watching Tours: Best Places & Time" 2) costa-rica-guide.com 3) tripadvisor.com "15 BEST … Whale Watching Tours" 4) iwc.int Handbook 5) puravidamoms.com "…Whale Watching in Uvita" 6) getyourguide.com. **CR**: 1) ballenainfocenter.com (Uvita de Osa) 2) ballenatourcostarica.com (Bahia Ballena) 3) dolphintourcostarica.com "Temporada de Ballenas Jorobadas — Uvita" 4) tripadvisor.es 5) FB "Festival de Ballenas — Uvita" 6) richcoastdiving.com. Real PAA — US: "When can I see whales in Costa Rica?", "Where is the best place to see whales?", "Is whale watching worth it?"; CR: "Cuando es la temporada…?", "Cual es el mejor lugar…?", "Cuanto cuesta ver ballenas…?". Questions endpoint (`whale watching`, us): "when is whale watching season" 390 vol, "where to whale watch" 480 — season/where intent dominates.
- **Content gap found:** First FAQ for whale-watching. Top rankers (all Uvita / Marino Ballena, South Pacific tour operators) consolidate direct answers to **when is the season / where best / is it guaranteed / worth it / cost**; our page had season + regional framing in prose (Overview, Whale season, Why Playa Hermosa, Family) but NO direct-answer FAQ block and NO FAQPage JSON-LD — weak for the live ai_overview/PAA. CONFLATION CAUTION: the prime sighting hub is Uvita/Bahia Ballena (Osa, South Pacific), NOT Playa Hermosa de Jaco — never claim Playa Hermosa is the best/prime spot.
- **Change (content):** Added a 4-item `faq?` to the whale-watching entry in `content/experiences.ts:175` (inserted before `cta`). Renders on-page (`detail-template.tsx:308`) AND emits FAQPage JSON-LD (`app/[locale]/experiences/[slug]/page.tsx:68-69`). Q&A (both locales, accent-free ES to match THIS entry — "Pacifico", "Jaco", "garantizados"): (1) "When is whale watching season in Costa Rica?" — July–October humpback season, conditions vary (Whale season + facts, verbatim window). (2) "Can you see whales from Playa Hermosa?" — sightings are OUT AT SEA along the central Pacific, Playa Hermosa is a beachfront STARTING POINT; team guides availability/timing/local options (Overview + Whale season "local options" verbatim). (3) "Are sightings guaranteed?" — explicit NO, wild nature activity, July–Oct improves chances (honest guarantee answer + PAA capture). (4) "Is it a good activity for families?" — educational, return to hotel after (Family section verbatim).
- **Truth check (nature/seasonal framing):** Season = the entry's own verbatim "July to October" — deliberately did NOT import the real-world Dec–April northern-humpback window (genuinely true but it CONTRADICTS this page's eyebrow/facts/Overview; self-contradiction avoided). NO resort-operated tours/boats/guides claimed — kept at the entry's exact support level ("team can guide on availability, recommended timing and local options" / arranged locally from the beachfront base). Explicit "not guaranteed" Q (Q3). Regional framing preserved: Playa Hermosa = base/starting point, whales out in the Pacific. Did NOT borrow the Uvita/Marino Ballena prime-spot framing from rankers (Osa/South Pacific = different region). Geo verified vs `lib/seo.ts` NAP (Playa Hermosa, Jaco, Puntarenas, Central Pacific); Guanacaste Playa Hermosa NOT conflated. Typecheck clean.
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → Experiences):** (1) Does the resort book/arrange a specific whale-watching tour partner for guests, or only point to "local options"? (rankers all name an operator + price; entry only says "local options"). (2) Approximate cost of a whale-watching tour from the area (CR PAA "Cuanto cuesta ver ballenas…?" — guardrail blocks invention).
- **Next candidate:** `turtle-nesting` FAQ (frame regionally/seasonal; body says Playa Hermosa IS a real nesting refuge but hedges "Seasonal and variable" — base, not guaranteed); then `live-music` (last experience with no FAQ, genuinely on-site at Vivace, low volume).

### Loop 18 (SERP-aware) — 2026-06-24 — Turtle Nesting detail (`/experiences/turtle-nesting`) — FAQ
- **Target keyword + SERP (region):** `sea turtle costa rica` 480 vol / KD 13 (EN, intent I) — chosen over `live-music` (far lower value, per Loops 7/16/17). Live SERP via `DATA_getSerpResults` (`advanced`, **US** DB, crawled 2026-06-25). SERP features: **ai_overview + people_also_ask + video + reviews + images + related_searches** (strong FAQ/AEO target). TOP organic: 1) costaricaexperts.com "Best Costa Rica Sea Turtle Nesting Sites: Where To Go When" 2) mytanfeet.com "Where to See Sea Turtles in Costa Rica" 3) seaturtlestatus.org "Costa Rica — Superlative Sea Turtle Country" 4) goeco.org "Volunteering with Sea Turtles" 5) conserveturtles.org (STC Tortuguero) 6) tortugueroinfo.com 7) pura-aventura.com "best turtle nesting spots". Real questions (`DATA_getKeywordQuestions`, us): "when do sea turtles hatch in costa rica" 50 (I), "where to see sea turtles in costa rica" 40 (**L+C**), "when do sea turtles lay eggs in costa rica" 10 (I). Dominant intent = **when (season/hatching) + where + is it guaranteed**.
- **Content gap found:** First FAQ for turtle-nesting. Rankers + the AI Overview consolidate direct answers to **when is nesting season / where to see them / how regulated/ethical**; our page had season hedge + conservation/responsible-viewing framing in prose (Overview, Conservation, Nesting and release, Responsible viewing) but NO direct-answer FAQ block and NO FAQPage JSON-LD. CONFLATION CAUTION: the AIO is built on Tortuguero (Caribbean) + Ostional/Las Baulas (Guanacaste) + a "March–October" range for THOSE sites — a different region from Playa Hermosa de Jaco; deliberately NOT borrowed.
- **Change (content):** Added a 4-item `faq?` to the turtle-nesting entry in `content/experiences.ts` (inserted before `cta`, after line 363). Renders on-page (`detail-template.tsx:308`) AND emits FAQPage JSON-LD (`app/[locale]/experiences/[slug]/page.tsx:68-69`). Q&A (both locales, accent-free to match THIS entry — "Jaco", "anidacion", "Pacifico"): (1) "When is sea turtle nesting season?" — seasonal/variable, depends on local conservation projects, NO fixed dates, ask before travel (entry's own hedge + CTA "Ask about the season"). (2) "Can you see turtle nesting at Playa Hermosa?" — yes, Playa Hermosa (south of Jaco, central Pacific) is a coastal nesting refuge; from our beachfront base, observe responsibly when activities are available (Loop 7 verified refuge status). (3) "Are turtle sightings guaranteed?" — explicit NO, wild nature activity (mirrors whale-watching Q3). (4) "How do you watch nesting responsibly?" — distance, avoid bright white lights, follow local conservation-project guidance, night viewing in CR usually needs certified guides; leave the beach better (Responsible viewing section verbatim).
- **Truth check (nature/seasonal framing):** CRITICAL — deliberately did NOT import the AIO's "March to October" (or any month range): the entry states no months ("Variable por temporada / Seasonal and variable"), and the AIO range describes Caribbean/Guanacaste sites, NOT Playa Hermosa de Jaco — a double violation avoided (matches the Loop 17 refusal to import a contradicting whale window). The "certified guide / regulated" AIO fact used ONLY as general-CR responsible-viewing/ethics framing, NOT as a resort-run/arranged tour (the entry doesn't claim one). Explicit "not guaranteed" Q3. Geo: every "where" answer kept on Playa Hermosa de Jaco / central Pacific (refuge status verified Loop 7); did NOT borrow Tortuguero/Ostional/Las Baulas. Geo verified vs `lib/seo.ts` NAP (Playa Hermosa, Jaco, Puntarenas, Central Pacific); Guanacaste Playa Hermosa NOT conflated. Typecheck clean.
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → new "Turtle Nesting" Experiences section):** (1) Exact local nesting/hatching season MONTHS at Playa Hermosa (top "when do sea turtles hatch/lay eggs" questions; entry only says "seasonal and variable" — guardrail blocks importing the AIO's competitor/region range). (2) Does the resort arrange a specific local conservation-project partner / guided (night) viewing for guests, or only point to local options?
- **Next candidate:** `live-music` is the LAST experience detail with no FAQ (genuinely on-site at Vivace Beachfront, weekends — low volume but safe, on-site framing OK). After that, all experience/event details have FAQs — rotate to hub-intro enrichment or revisit title/description loops.

### Loop 19 (SERP-aware) — 2026-06-25 — Live Music detail (`/experiences/live-music`) — FAQ
- **Target keyword + SERP (region):** `live music jaco costa rica` (live SERP via `DATA_getSerpResults`, `advanced`, **US** DB — note: `DATA_getKeywordsMetrics` confirms low volume: `live music costa rica` 20 vol / KD 6 intent **L+C**, `jaco nightlife` 10 vol / KD 9; `live music jaco costa rica` / `live music playa hermosa` / `things to do playa hermosa jaco` all `is_data_found:false`; questions endpoint 429'd "too many requests"). So this loop is a completeness + on-site truth play, justified by content gap + live SERP, not a volume number. SERP features present: **people_also_ask + related_searches + reviews + video + local_pack** (no AI Overview). TOP organic: 1) greenroomjaco.com "Green Room Café | Live Music!" (live music every night) 2) jacovip.com "Jaco Nightlife — VIP Clubs & Party Planning" 3) jacopartyvillas.com "Ultimate 2025 Guide to Jaco's Party Scene" 4) tripadvisor.com Jaco attractions 5) **opentable.com "Restaurants with live music in Jaco, Puntarenas"** (most on-purpose: restaurant + live music) 6) jacobar.com 7) republikcostarica.com/events. PAA: "Does Jaco have good nightlife?", "What is the most popular music in Costa Rica?" (reggaeton), "What town in Costa Rica has a fun nightlife?", "Is Hotel Cocal Jaco adult only?".
- **Content gap found:** First (and last) FAQ for live-music. Rankers + opentable answer "when / what kind of music / where / is it a restaurant-with-live-music or a club / can I walk in." Our page had rich prose (Overview, Weekend atmosphere, Restaurant and bar connection, Local artists, Dinner+cocktails+sunset) but NO direct-answer FAQ block and NO FAQPage JSON-LD. CONFLATION/INTENT CAUTION: the SERP is dominated by Jaco **nightlife/party-scene/club** framing (jacovip, jacopartyvillas, "reggaeton", "dance clubs") — our entry is deliberately a *relaxed, family-friendly, on-site at Vivace* experience, NOT a nightclub; turned that contrast into a differentiator (Q2 explicitly "not a nightclub") rather than borrowing the party-town facts.
- **Change (content):** Added a 4-item `faq?` to the live-music entry in `content/experiences.ts` (inserted before `cta`, ~line 742). Renders on-page (`detail-template.tsx:308`) AND emits FAQPage JSON-LD (`app/[locale]/experiences/[slug]/page.tsx:68-69`). Q&A (both locales, accent-free to match THIS entry's convention — "musica", "Jaco", no opening ¿, per the entry's own prose + Loop 18 style): (1) "When is there live music in Playa Hermosa?" — weekends at Vivace Beachfront, lineup changes weekly, check the weekly schedule (Frequency fact + Local-artists section verbatim; did NOT invent exact days/times). (2) "What kind of music is played?" — local artists, relaxed sound for dinner/cocktails/family atmosphere; **explicitly "not a nightclub"** (Overview + Local-artists sections; refuses the reggaeton/club PAA import). (3) "Where is the live music at the resort?" — Vivace Beachfront, connects with Iguana Bar + Golden Beach Bar (Restaurant-and-bar-connection section verbatim). (4) "Can non-guests visit for the live music?" — Vivace welcomes guests AND outside visitors, with or without a reservation, resort guests seated first; book ahead for sunset/weekends — **this is the on-purpose answer for the opentable "restaurants with live music in Jaco" searcher**.
- **Truth check:** Q1/Q2/Q3 trace verbatim to the entry's own facts/sections (Frequency=Weekends, Place=Vivace Beachfront; Overview, Local artists, Restaurant and bar connection). Q4 public-access RESOLVED via grep (NOT client-input): `content/restaurant-page.ts:55` = "Walk-ins & reservations" / "Con o sin reserva" and `:121` = "Resort guests dine first" / "Los huéspedes del resort cenan primero" — so non-guest access + guests-first is a repo fact. Did NOT misread "enjoy the evening without leaving the resort" (convenience framing) as guests-only. Did NOT import the SERP's nightlife/club/reggaeton facts. Did NOT invent exact weekend days/times or any cover/music charge. Geo (Playa Hermosa, south of Jaco, central Pacific) verified vs `lib/seo.ts` NAP; Guanacaste Playa Hermosa NOT conflated. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → Experiences):** (1) Exact weekend days/times of live music (page + FAQ only say "weekends / check the weekly lineup"). (2) Is there any cover charge or music/minimum-spend for the live-music nights, or is it just regular Vivace dining?
- **Next candidate:** ✅ **This COMPLETES the detail-page FAQ pass — ALL 6 event details + all 5 experience details now have FAQs + FAQPage JSON-LD.** Rotate to a new page type: (1) the **Experiences hub** (`/experiences`) or **Events hub** intro — geo+intent-enrich + consider a hub-level FAQ; (2) the **Restaurant / Vivace** page (`/restaurant`) — strong commercial page, could get a "live music / hours / walk-ins / reservations" FAQ from the same repo facts; (3) the **Bars** or **Suites hub** page; (4) revisit hub META titles/descriptions.

### Loop 20 (SERP-aware) — 2026-06-25 — Restaurant/Vivace (`/restaurant`) — META DESCRIPTION
- **Target keyword + SERP (region):** Commercial restaurant cluster, **US** DB (`DATA_getKeywordsMetrics` + `DATA_getSerpResults` advanced for `beachfront restaurant jaco costa rica`). Metrics: **`best restaurants jaco` 480 vol / KD 4, intent L+C** (superlative — not literally claimable); **`restaurants jaco beach` 170 vol / KD 6, L+C**; `restaurants playa hermosa costa rica` 40 / KD 6, L+C; `restaurants in jaco costa rica` / `jaco restaurants` 10 / KD 8, L+C; `mediterranean restaurant jaco` / `italian restaurant jaco costa rica` / `beachfront restaurant jaco` all `is_data_found:false`. SERP features present: **organic + related_searches** (no AI Overview/PAA on this query). TOP organic: 1) twoweeksincostarica.com "Jaco Restaurant Guide" 2) tripadvisor.com "Restaurants in Jaco" 3) reddit.com r/CostaRicaTravel "jaco restaurants" 4) puravidamoms.com "Restaurants in Jaco" 5) hoteljaco.com "restaurant jaco beach" 6) beachfronthoteljaco.com 7) tripadvisor "Seasalt **Beachfront** Bar & Restaurant — Jaco" 8) mytanfeet.com "Restaurants in Jaco" 9) seasaltjacobeachcr.com 10) cocobeachfront.com. Dominant SERP anchors = **Jaco + beachfront**.
- **Content gap found:** Every high-volume L+C term and nearly every top ranker is anchored on **"Jaco"** (best restaurants jaco 480, restaurants jaco beach 170) + **beachfront**, yet our metadata targeted only "Playa Hermosa." The TITLE already carries "Playa Hermosa" and is already 61 chars (at the cap) — adding ", Jaco" would push it to ~67 or force dropping the disambiguator. The META DESCRIPTION was the genuinely weakest geo link: it named **no town at all** ("Beachfront restaurant led by Sicilian Chef Luigi … fresh Costa Rican ingredients" — "Costa Rican" was only ingredient origin) and was short (EN 119 chars) with headroom to ~150. So enriched the description, not the title.
- **Change (meta description, both locales, 2 files):** EN — `content/restaurante.json:4` (`data.desc`, feeds Next metadata `description`/OG via `app/[locale]/restaurant/page.tsx` `seoByLocale.en` → `pageMetadata`): "Beachfront restaurant led by Sicilian Chef Luigi. Mediterranean and Italian cuisine with fresh Costa Rican ingredients." → "Beachfront restaurant **in Playa Hermosa de Jacó, Costa Rica**, led by Sicilian Chef Luigi. Mediterranean and Italian cuisine with fresh **local** ingredients." (151 chars; leads with "Beachfront", adds the Jacó geo anchor, swaps "Costa Rican ingredients" → "local ingredients" so "Costa Rica" now reads as true location not ingredient-origin). ES — `app/[locale]/restaurant/page.tsx:21` (`seoByLocale.es.desc`, inline): "Restaurante frente al mar a cargo del chef siciliano Luigi. Cocina mediterránea e italiana con ingredientes frescos de Costa Rica." → "Restaurante frente al mar **en Playa Hermosa de Jacó, Costa Rica**, a cargo del chef siciliano Luigi. Cocina mediterránea e italiana con ingredientes frescos." (154 chars; "Jacó" accented to match the ES title's convention). Title tags left unchanged (already carry geo + cuisine + at length cap).
- **Truth check:** Adding "Jacó" *strengthens* truth — "Playa Hermosa **de Jacó**" is the correct full place name and actively DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa the log guards against. Verified vs `lib/seo.ts` `restaurantJsonLd` address (addressLocality "Jacó", Puntarenas, CR) + the page's own hero copy. "Beachfront" verified (resort is beachfront on Playa Hermosa; `hero.titleLines` "Pacific Setting", `meta` walk-ins). Cuisine "Mediterranean and Italian" verified vs `servesCuisine` + Chef Luigi (Sicilian) copy. NO invented dishes, prices, ratings, or awards. Did NOT claim the 480-vol "best restaurants jaco" superlative (unclaimable). Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → new "Restaurant — Vivace Beachfront" section):** (1) Restaurant `priceRange` / price tier ($–$$$ or avg per-person) — Google surfaces `priceRange` in the Restaurant rich result and `restaurantJsonLd` currently omits it; guardrail blocks inventing a number.
- **Next candidate:** (1) `restaurantJsonLd` `acceptsReservations: true` — VERIFIABLE from repo (hero `meta` "Walk-ins & reservations" / "Con o sin reserva"; reserve section "Dinner reservations recommended") — a clean schema add for the next loop. (2) A Restaurant-page `faq?`-style block (hours / walk-ins vs reservations / live music / guests-first) from repo facts — note: this is a BESPOKE page, so `faq?` is NOT wired; would need a hand-built FAQ + manual FAQPage JSON-LD, FLAG as a feature-add. (3) Suites hub or Bars page META titles/descriptions (next commercial pages).

### Loop 21 (SERP-aware) — 2026-06-25 — Suites/Rooms hub (`/suites`) — META TITLE
- **Target keyword + SERP (region):** Lodging cluster, **US** DB (`DATA_getKeywordsMetrics` + `DATA_getSerpResults` advanced for `beachfront hotel playa hermosa jaco costa rica`). Metrics: **`playa hermosa costa rica hotel` 390 vol / KD 10 (I)**; **`hotels playa hermosa costa rica` 140 vol / KD 8, intent L+C**; `playa hermosa jaco hotels` 10 / KD 7, L+C; `jaco beach hotel` 10 / KD 12 (I); `beachfront hotel playa hermosa jaco` / `beachfront hotel jaco costa rica` / `where to stay playa hermosa jaco` all `is_data_found:false`. SERP features present: **hotel_pack + organic + related_searches** (no AI Overview/PAA). TOP organic: 1) backyardhotel.com "Backyard Hotel **Playa Hermosa - Jaco** Beach, Costa Rica" 2) hbhcr.com "Hermosa **Beach Front** Hotel in Costa Rica" 3) tripadvisor.com "THE 10 BEST Hotels in Playa Hermosa, Costa Rica" 4) booking.com "10 Best Playa Hermosa Hotels (From US$85)" 5) hotels.com "10 Best **Beach** Hotels in Playa Hermosa" 6) agoda.com "11 Best Hotels in **Playa Hermosa, Jaco**" 7) instagram backyardhotelcr (Puntarenas) 8) jacohotelspage.com "THE BACKYARD **BEACHFRONT** HOTEL (JACO)" 9) mytanfeet "Playa Hermosa (Jaco)" 10) expedia "Travel Guide for Playa Hermosa, **Jacó**". Related searches: "Beachfront hotels in playa hermosa costa rica". Dominant SERP anchors = **Playa Hermosa + Jaco + beachfront**.
- **Content gap found:** Every top ranker and the related-search cluster title-tag **"Playa Hermosa" + "Jaco/Jacó" + "Beachfront/Beach Front"** — yet the suites-hub `<title>` carried "Beachfront rooms and villas" with **ZERO geo** (geo lived only in the meta DESCRIPTION, which `<title>` outweighs). The title was the page's weakest geo link. NOTE: `roomsJsonLd` is NOT even emitted on this hub (page.tsx emits only `breadcrumbJsonLd`); the meta title is the highest-leverage field. The hub H1 ("Wake to the Pacific, Four Ways") comes from `hero.titleLines` and the breadcrumb from `dict.suites.title` — so `seoCopy.title` feeds ONLY the metadata `<title>`/OG (no H1/breadcrumb/CTA side effect — contained meta-only change, unlike events/experiences `content.ts` titles).
- **Change (meta title, both locales, 1 file — `app/[locale]/suites/page.tsx`):** EN `seoCopy.en.title:20` — "Beachfront rooms and villas | Hotel Terraza del Pacífico" (56 chars) → "Beachfront Rooms & Villas in Playa Hermosa de Jacó | Terraza" (60 chars). ES `seoCopy.es.title:16` — "Habitaciones y villas frente al mar | Hotel Terraza del Pacífico" → "Habitaciones frente al mar en Playa Hermosa de Jacó | Terraza" (61 chars). Both now front-load lodging-term + "frente al mar"/"Beachfront" + the full "Playa Hermosa de Jacó" geo BEFORE the pipe; brand trimmed from "Hotel Terraza del Pacífico" → "Terraza" to keep geo inside the ~60-char truncation budget (verified with a char-count script).
- **Truth check:** Adding "de Jacó" STRENGTHENS truth and DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa — identical safe move to Loop 20. Verified vs `lib/seo.ts` (`addressLocality: "Jacó"`, `streetAddress: "Playa Hermosa, 34"`, Puntarenas, CR). "Beachfront" verified (resort on the beach; `included` items "0 m to the sand", "The resort is on the beach"). "Rooms & Villas" = the four real types (junior-suite/superior/estándar/villas per `hubRoomOrder` + `lib/seo.ts`). NO invented price, room count, size, or rating. Did NOT claim "best hotel" superlatives or the booking.com "From US$85" price. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → new "Suites / Rooms hub" section):** (1) Starting nightly rate / price range for the rooms & villas — competitors rank with "From US$85" in the SERP title and the hub/`roomsJsonLd` carry no price; guardrail blocks inventing a number (consistent with Loop 20's `priceRange` flag).
- **Next candidate:** (1) Emit `roomsJsonLd` (ItemList of HotelRoom) on the suites hub page — currently only `breadcrumbJsonLd` is emitted, a clean verifiable schema add (occupancy/floorSize/beds already parsed from dict, no price). (2) Suites hub META DESCRIPTION — could add "de Jacó" to the existing "Playa Hermosa" (weaker win, desc already has geo). (3) Back to Restaurant `acceptsReservations: true` schema add; or the Bars page META title/description.

### Loop 22 (SERP-aware) — 2026-06-25 — Bars hub (`/bars`) — META TITLE
- **Target keyword + SERP (region):** Bar cluster, **US** DB (`DATA_getKeywordsMetrics` + `DATA_getSerpResults` advanced for `beach bar jaco costa rica`). Metrics: **`bars jaco costa rica` 170 vol / KD 8, intent L+C** (the standout — exact match to what this hub IS); `beach bar costa rica` 30 / KD 13, L+C; `beach bar jaco costa rica` / `beachfront bar jaco` / `beach bar playa hermosa` / `jaco beach bars` / `pool bar costa rica` / `swim up bar costa rica` / `best bars jaco` all `is_data_found:false`. SERP features present (for `beach bar jaco costa rica`): **ai_overview + people_also_ask + reviews + related_searches + gmb + video** (strong AEO/local target). `DATA_getKeywordQuestions` (`bars jaco costa rica`, us) returned 0. TOP organic: 1) tripadvisor.com "BEACH BAR, Jaco — Restaurant Reviews" 2) facebook "best beach bars and restaurants in Jaco?" 3) bestwesternjacobeach.com "El Muelle Bar | Jaco Costa Rica Restaurants" 4) instagram JacoBar 5) jacovip.com "Jaco Nightlife — VIP Clubs & Party Planning" 6) centralamerica.com "Seven Jaco Beach Bars You Need to Check Out" 7) tripadvisor "Clarita's Beach Bar & Resort — Jaco" 8) puravidacasas.com 9) beachfronthoteljaco.com 10) reddit "Jaco Bars and Night Clubs". AI Overview PAA: **"What is the nicest beach near Jaco, Costa Rica? Tell me more about Playa Hermosa"** — Google itself ties Playa Hermosa to the "beach near Jaco" frame. Related searches: heavy nightlife/club intent ("Best clubs in Jaco", "Jaco beach club", "Monkey Bar"). Dominant SERP anchors = **Jaco + bars + beach(front)**.
- **Content gap found:** Every high-value term and top ranker is anchored on **"Jaco" + bars/beach**, yet the bars-hub `<title>` ("Bares · Golden Beach Bar e Iguana Bar | Hotel Terraza del Pacífico") carried **ZERO geo** — it spent its whole 65-char budget on two low-volume venue proper-nouns + brand, with "Playa Hermosa" living only in per-venue hero `meta`, never in the hub title (the highest-weight field). The title was the page's weakest geo link. NOTE: `barsItemListJsonLd` is defined in `lib/seo.ts` but is **NOT emitted** on the hub (`app/[locale]/bars/page.tsx` emits only `breadcrumbJsonLd`) — direct parallel to Loop 21's un-emitted `roomsJsonLd`; the meta title is the highest-leverage field this loop. The hub H1 comes from `barsIndexCopy[l].title` ("Two bars, both on the water") and the breadcrumb from an inline "Bars"/"Bares" string — so `seo.title` feeds ONLY the metadata `<title>`/OG (no H1/breadcrumb/CTA side effect; contained meta-only change).
- **Change (meta title, both locales, 1 file — `app/[locale]/bars/page.tsx`):** EN `seo.en.title:20` — "Bars · Golden Beach Bar & Iguana Bar | Hotel Terraza del Pacífico" (65 chars) → "Bars in Playa Hermosa de Jacó, Costa Rica | Terraza" (51 chars). ES `seo.es.title:16` — "Bares · Golden Beach Bar e Iguana Bar | Hotel Terraza del Pacífico" → "Bares en Playa Hermosa de Jacó, Costa Rica | Terraza" (52 chars). Both now front-load the **exact 170-vol L+C term** ("Bars … Jacó, Costa Rica" / "Bares … Jacó, Costa Rica") + the full "Playa Hermosa de Jacó" geo before the pipe; venue proper-nouns dropped (they survive in the H1 card H2s + breadcrumb + future schema), brand trimmed "Hotel Terraza del Pacífico" → "Terraza" to keep geo inside the ~60-char budget (verified with a char-count script).
- **Truth check:** Used **"Bars" (not "Beach Bars")** — only Golden Beach Bar is on the sand; Iguana Bar is a **pool bar** (the page's own copy: "two bars on the water" / "waterfront"), so "Beach Bars" plural would overclaim. "Bars" both avoids the overclaim AND matches the real money term (`bars jaco costa rica` 170 vol; `beach bar` only 30). "Playa Hermosa **de Jacó**" STRENGTHENS truth and DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa — verified vs `lib/seo.ts` (`addressLocality: "Jacó"`, `streetAddress: "Playa Hermosa, 34"`, Puntarenas, CR). Both bars are genuinely on-property (Golden Beach Bar beachfront, Iguana Bar poolside per `content/bars.ts`). Did NOT adopt the SERP's nightlife/club/party-town framing (jacovip, "best clubs", "beach club") — kept the relaxed beachfront-resort-bar framing per guardrail. NO invented prices, drink menus, happy-hour/event times, or ratings. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT (added to `docs/client-questions-seo.md` → new "Bars" section):** (1) Drink/menu prices for the two bars — `content/bars.ts` intentionally leaves every pour `price` blank "until confirmed"; the Jaco bar SERP rankers all surface menus/prices and the AI Overview leans on them. With confirmed prices we can render a pour list and add `priceRange` to the bar schema (consistent with the Restaurant/Suites `priceRange` flags from Loops 20–21).
- **Next candidate:** **All 3 commercial pages (Restaurant, Suites hub, Bars hub) now have geo-tagged titles/descriptions.** Highest-leverage next move: (1) **Emit `barsItemListJsonLd` on the bars hub** (`app/[locale]/bars/page.tsx` currently emits only `breadcrumbJsonLd`) — the helper already exists in `lib/seo.ts` and the SERP has ai_overview + gmb + reviews, so structured BarOrPub nodes (hours already in `content/bars.ts`) are a clean verifiable schema add; same un-emitted-helper pattern as Loop 21's `roomsJsonLd`. (2) Emit `roomsJsonLd` on the suites hub (still pending from Loop 21). (3) Restaurant `acceptsReservations: true` schema add (pending from Loop 20). (4) Bars hub META DESCRIPTION — add "de Jacó" geo (weaker; desc currently names no town, only "Terraza del Pacífico"). Suggest a **schema-emission pass** next (bars + rooms ItemList, restaurant acceptsReservations) now that title/description geo is covered across all commercial pages.

### Loop 23 (SERP-aware) — 2026-06-25 — Suites hub (roomsJsonLd schema)
- **Type:** SCHEMA-EMISSION fix (no keyword/meta-copy change). Closes the gap flagged in Loops 21–22: `roomsJsonLd` was DEFINED in `lib/seo.ts` but NEVER emitted on its hub (the suites hub emitted only `breadcrumbJsonLd`).
- **What was wired (`app/[locale]/suites/page.tsx`, 1 file):** Imported `roomsJsonLd` alongside the existing `breadcrumbJsonLd`/`pageMetadata`, and emitted `<JsonLd data={roomsJsonLd(l, rooms)} />` right after the breadcrumb JsonLd — matching the restaurant page's working `<JsonLd data={restaurantJsonLd()} />` pattern (same `@/components/json-ld` import already in the file). `rooms` is built from the SAME on-page data the hub renders: `dict.suites.items` (locale-aware via `getDictionary(l)`), mapped to `RoomListEntry`.
- **Room fields MAPPED (all four real display strings, present for every room in both locales):** `slug` ("superior"/"estandar"/"junior-suite"/"villas"), `name` (e.g. "Superior Room" / "Habitación Superior"), `guests` ("4"/"4"/"2"/"6" → parsed to `occupancy.maxValue`), `size` ("32 m²"/"32 m²"/"32 m²"/"72 m²" → parsed to `floorSize.value` MTK), `beds` ("2 Double"/"2 Double"/"1 King"/"Multiple" → `bed`). roomsJsonLd builds each HotelRoom `url` via `localizedPath(locale, suites/<slug>)`, matching the hub's own per-room links (`suites-hub.tsx:383`).
- **Fields OMITTED (on purpose):** `price` — roomsJsonLd never emits it (guardrail; fabricated price = penalty risk). "Multiple"/"Múltiples" beds emit the raw string with no fabricated count (the helper only adds a count when a leading number is present). No occupancy/size guessing — every value came from the dict.
- **Truth check:** Every emitted value is read verbatim from `dict.suites.items` (the strings the page already shows on screen). NO invented occupancy, size, beds, price, or rating. Geo is inherited from `absoluteUrl`/`localizedPath` (no new geo claim added). Verified the emission mirrors the restaurant page so it renders. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT:** (1) Starting nightly rate / price range per room — would let `roomsJsonLd` carry `priceRange`/Offer (currently omitted by design); already logged under Suites in `docs/client-questions-seo.md` (Loop 21). No NEW gap: guests/size/beds are all present, so occupancy + floorSize already emit truthfully.
- **Next candidate:** **Emit `barsItemListJsonLd` on the bars hub** (`app/[locale]/bars/page.tsx`, still emits only `breadcrumbJsonLd`) — same un-emitted-helper pattern just closed here; hours already in `content/bars.ts`, SERP has ai_overview + gmb + reviews. Then Restaurant `acceptsReservations: true` schema add (pending from Loop 20).

### Loop 24 (SERP-aware) — 2026-06-25 — Bars hub (barsItemListJsonLd schema)
- **Type:** SCHEMA-EMISSION fix (no keyword/meta-copy change). Closes the LAST unemitted-helper gap flagged in Loops 22–23: `barsItemListJsonLd` was DEFINED in `lib/seo.ts` but NEVER emitted on its hub (the bars hub emitted only `breadcrumbJsonLd`).
- **What was wired (`app/[locale]/bars/page.tsx`, 1 file):** Imported `barsItemListJsonLd` alongside the existing `breadcrumbJsonLd`/`pageMetadata`, and emitted `<JsonLd data={barsItemListJsonLd(l, barList)} />` right after the breadcrumb JsonLd — matching the restaurant page's `<JsonLd data={restaurantJsonLd()} />` and the Loop-23 suites `<JsonLd data={roomsJsonLd(l, rooms)} />` working pattern (same `@/components/json-ld` import already in the file). `barList` is built from the SAME on-page venue data the hub renders: the `bars` array from `content/bars.ts`, mapped to `BarListEntry`.
- **Bar fields MAPPED (both venues, both locales):** `slug` ("golden-beach-bar"/"iguana-bar"), `name` (`venue.text[l].hero.title` → "Golden Beach Bar"/"Iguana Bar"; locale-aware though both are proper nouns), `cardImage` (each venue's curated cover → `image` absoluteUrl), and `hours` — BOTH venues carry real `{opens,closes}` in `content/bars.ts` (Golden Beach Bar 11:00–19:00, Iguana Bar 10:00–18:00), so `openingHoursSpecification` (Mo–Su) emits for both. The helper builds each BarOrPub `url` via `localizedPath(locale, bars/<slug>)` (matches the hub's own per-venue links) + `parentOrganization` `@id` back to `#hotel`.
- **Fields OMITTED (on purpose):** drink menus / pour prices / ratings — none confirmed in repo (`content/bars.ts` leaves every pour `price` blank "until confirmed"); the helper has no price/menu surface anyway. The `hours` guard (`...(venue.hours ? ...)`) is in place for safety even though both venues happen to have hours, so a future hours-less venue stays truthful.
- **Truth check:** Every emitted value read verbatim from `content/bars.ts`. Iguana represented as the POOL bar and Golden Beach Bar as the beach bar — no relabel (only slug/name/image/hours emitted, so no venue-type claim is made in the schema at all). NO invented prices, menus, happy-hour times, or ratings. Hours are the venues' own published daily windows. Resort geo is handled inside the helper (`absoluteUrl`/`localizedPath`); no new geo claim added. Playa Hermosa de Jacó, Puntarenas (NOT Guanacaste) — unchanged. Verified the emission mirrors the suites/restaurant pattern so it renders. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT:** No NEW gap — both venues' hours are present in content, so `openingHoursSpecification` emits truthfully for both. The pre-existing Bars gap (confirmed drink/pour prices → enables a pour list + `priceRange` on the bar schema) remains logged under "Bars" in `docs/client-questions-seo.md` (date bumped to Loop 24).
- **Next candidate:** ✅ **All 3 schema helpers now emit** — `restaurantJsonLd` (restaurant), `roomsJsonLd` (suites hub, Loop 23), `barsItemListJsonLd` (bars hub, this loop). The un-emitted-helper pass is COMPLETE. Highest-leverage next moves: (1) **Restaurant `acceptsReservations: true`** schema add to `restaurantJsonLd` — VERIFIABLE from repo (hero `meta` "Walk-ins & reservations" / "Con o sin reserva"; reserve section "Dinner reservations recommended"); pending since Loop 20. (2) A second **META pass** — hub META DESCRIPTIONS (bars desc names no town; add "de Jacó" geo), or revisit any remaining geo-thin title/description. (3) The **feeder-market / Google-Ads landing-page build** (a net-new page targeting the high-intent commercial clusters surfaced across Loops 20–22).

### Loop 25 (SERP-aware) — 2026-06-25 — Vivace MENU page (`/restaurant/menu`) — META TITLE + DESCRIPTION
- **Target keyword + SERP (region):** Menu/cuisine cluster, **US** DB (`DATA_getSerpResults` advanced + `DATA_getKeywordsMetrics`). Metrics: nearly all menu seeds `is_data_found:false` (`italian restaurant jaco costa rica`, `restaurant playa hermosa menu`, `jaco restaurant menu`, `italian food jaco costa rica` all no data); only **`vivace menu` 10 vol / KD 8, intent I** (branded/navigational — Vivace's own name). So this loop is justified by the live SERP + content gap, not a volume number. Live SERP for **`italian restaurant jaco costa rica`** — features present: **local_pack + reviews + related_searches + video** (no AI Overview/PAA). **local_pack** top 3 = Amancio's Pizza, Cibo Costa Rica, La Bruschetta Jaco (header "Puntarenas Province, Jaco, Costa Rica"). TOP organic: 1) tripadvisor "THE 10 BEST Italian Restaurants in Jaco (2026)" 2) facebook "What is the Italian restaurant in Jaco… with fresh…" 3) trattorialabruschetta.com "La Bruschetta | Trattoria Bar - Jacó" 4) tripadvisor "THE BEST Pasta in Jaco (June 2026)" 5) crocsresortcasino "La Stanza Fine Dining" 6) **facebook "Vivace italian restaurant #jaco #jacó #italianfood"** 7) remax-ocr "Iconic Italian Restaurant & Pizzeria in Jaco" 8) **instagram "Vivace • Italian Restaurant 🇮🇹 (@vivaceristorantecr) · Jacó"**. Related searches: **"Italian restaurant jaco costa rica menu"**, "Best/Authentic italian restaurant jaco costa rica", "Bruschetta Jacó Menu", "Pizza jaco costa rica". Dominant SERP anchors = **Jacó + Italian + menu**.
- **Content gap found:** The menu page `<title>` ("Menu · Vivace Beachfront | Hotel Terraza del Pacífico") carried **NEITHER geo NOR a cuisine word** — it spent its whole budget on "Vivace Beachfront" + brand. Yet the entire SERP (local pack + every ranker, incl. Vivace's OWN FB/IG which self-ID as "Italian Restaurant · Jacó") is anchored on **Italian + Jacó**, and **"italian restaurant jaco costa rica menu"** is a literal related search matching this exact page's intent. The DESCRIPTION named the cuisine only as "Mediterranean"/"mediterráneo" and named **no town** ("on the beach" / "en la playa" only). Both fields were the page's weakest links.
- **Change (meta title + description, both locales, 1 file — `app/[locale]/restaurant/menu/page.tsx`):** EN title `seo.en.title:45` — "Menu · Vivace Beachfront | Hotel Terraza del Pacífico" → "Vivace Menu · Italian Restaurant in Playa Hermosa de Jacó" (57 chars). ES title `seo.es.title:41` — "Menú · Vivace Beachfront | Hotel Terraza del Pacífico" → "Carta de Vivace · Restaurante italiano en Playa Hermosa, Jacó" (61 chars). EN desc `:46` → "The menu at Vivace Beachfront, our Mediterranean and Italian restaurant in Playa Hermosa de Jacó: house-made pastas, Pacific seafood, pizzas and dessert." (153 chars). ES desc `:42` → "Carta de Vivace Beachfront, nuestro restaurante mediterráneo e italiano en Playa Hermosa de Jacó: pastas caseras, pescados del Pacífico, pizzas y postres." (154 chars). Titles now front-load **Menu/Carta + Vivace** (branded `vivace menu` seed) + **Italian/italiano** (SERP-proven, weighted over Mediterranean) + the full **Playa Hermosa de Jacó** geo; descriptions add "Italian/italiano" + the Jacó geo. Dropped the "Beachfront"/"Terraza" brand tail from the titles to fit cuisine+geo inside the ~60-char budget (char-counted).
- **Contained-change check:** `seo.title/desc` feed `pageMetadata` (Next `<title>`/`description`/OG) **ONLY** — the breadcrumb is a hardcoded "Menu"/"Menú" (`page.tsx:81`) and `VivaceMenu` receives only `locale`/`initialCat`. No H1/breadcrumb/CTA side effect (verified in `page.tsx`). Menu JSON-LD + component copy left untouched (scope discipline).
- **Truth check:** "Italian" is verified — `menu-vivace.ts` carries real house-made pastas + pizzas; `servesCuisine` in `lib/seo.ts:281` = ["Mediterranean","Italian","Costa Rican"]; Vivace's OWN IG self-IDs as "Italian Restaurant · Jacó". "Playa Hermosa de Jacó" STRENGTHENS truth + DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa — verified vs `lib/seo.ts` (`addressLocality:"Jacó"`, `streetAddress:"Playa Hermosa, 34"`, Puntarenas, CR). "Pacific seafood"/"pescados del Pacífico", "pastas", "pizzas" all present in `menu-vivace.ts`. NO invented dishes/prices/ratings; did NOT claim the "best/authentic" superlatives from the SERP. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT:** **None new** — the menu already carries real prices (Costa Rican colones in `menu-vivace.ts`, fed into the Menu JSON-LD Offers), so no priceRange gap on this page. (The Restaurant-page `priceRange` flag from Loop 20 is unrelated and already logged.)
- **Next candidate:** (1) **Restaurant `acceptsReservations: true`** schema add to `restaurantJsonLd` (`lib/seo.ts`) — still pending since Loop 20, VERIFIABLE from repo ("Walk-ins & reservations" / "Con o sin reserva"; "Dinner reservations recommended"); the clean schema win. (2) Add `servesCuisine`/`hasMenu` link or a `Restaurant`-scoped `@id` cross-check between the Menu JSON-LD (`isPartOf` → `/restaurant#restaurant`) and `restaurantJsonLd` (verify the `@id` matches so the Menu associates correctly). (3) Bars hub META DESCRIPTION — add "de Jacó" geo (still names no town).

### Loop 26 (SERP-aware) — 2026-06-25 — Junior Suite detail (`/suites/junior-suite`) — META TITLE
- **Target keyword + SERP (region):** Room/lodging cluster, **US** DB (`DATA_getKeywordsMetrics` + `DATA_getSerpResults` advanced for `junior suite playa hermosa jaco costa rica`). Metrics: nearly all room-specific seeds `is_data_found:false` (`junior suite costa rica`, `junior suite jaco`, `beachfront suite jaco`, `suite playa hermosa`, `king suite costa rica`, `hotel room playa hermosa costa rica`, `suite frente al mar costa rica` all NO data); the only anchor is **`playa hermosa costa rica hotel` 390 vol / KD 10 (intent I)** — so this loop is justified by the live SERP + content gap, not a room-level volume number. SERP features present: **ai_overview + hotel_pack + organic + related_searches** (strong AEO/local target). TOP organic show competitors title their room pages EXACTLY this way: "Junior Suite - Playa Hermosa hotel Villa del Sueno" (villadelsueno.com), "Tiffany Playa Hermosa – Junior Suite", "La Playa (Beach Room) – Junior Suite" (lagaviotatropical), "VistaHermosa Junior Suite with Ocean Views" (airbnb), "Jr Suite Ocean View" (azuraresorts). **Our own page ranks with a bare "Junior Suite"** (terrazadelpacifico.com/habitaciones/junior-suite) — ZERO geo. "People also search for" cluster: "junior suite playa hermosa jaco costa rica reviews/price/cost" — searchers append the full "playa hermosa jaco costa rica" geo to "junior suite". GUANACASTE TRAP confirmed in-SERP: several rankers are the unrelated Guanacaste Playa Hermosa (Hotel Bosque del Mar, Villa del Sueño, Las Brisas Resort) — "de Jacó" is the disambiguator.
- **Content gap found:** The junior-suite detail `<title>` ("Junior Suite with pool view | Hotel Terraza del Pacífico" / "Junior Suite frente a la piscina | Hotel Terraza del Pacífico") carried **ZERO geo** — "Playa Hermosa" lived only in the meta DESCRIPTION, which `<title>` outweighs. The whole SERP + PAA is anchored on the room type + "Playa Hermosa de Jacó" geo, and our page ranks with the geo-free title. The title was the page's weakest geo link. junior-suite is the featured/highest-value room (couples, only 2 available, King). NOTE: `seo.title` feeds ONLY `pageMetadata` (`<title>`/OG) — the H1 + breadcrumb come from `data.h1[0]` ("Junior Suite") and `SuiteDetail` gets only slug/locale/dict, so this is a contained meta-only change (no H1/breadcrumb/CTA side effect, same as Loop 21).
- **Change (meta title, both locales, 1 file — `app/[locale]/suites/junior-suite/page.tsx`):** EN `seo.en.title:22` — "Junior Suite with pool view | Hotel Terraza del Pacífico" (56 chars) → "Junior Suite with Pool View in Playa Hermosa de Jacó" (52 chars). ES `seo.es.title:18` — "Junior Suite frente a la piscina | Hotel Terraza del Pacífico" → "Junior Suite frente a la piscina en Playa Hermosa de Jacó" (57 chars). Both now front-load the room type + its real differentiator (**Pool View / frente a la piscina**) + the full "Playa Hermosa de Jacó" geo; brand tail dropped to fit geo inside the ~60-char budget (char-counted). Description left unchanged (already carries "Playa Hermosa").
- **Truth check (BEACHFRONT TRAP AVOIDED):** Deliberately did NOT apply the generic "beachfront suite" recipe — this room's defining feature per its own content (`content/habitaciones-junior-suite.json`) is the **main-POOL view** ("A top-floor suite for two, above the main pool", "Main pool view | Upper floor | Only 2 rooms available", "look straight over the lit main pool"), NOT an ocean/beach view. Calling it "Beachfront/Ocean-view Junior Suite" would invent a view (guardrail violation). Led with "Pool View / frente a la piscina" — verbatim from content. "Junior Suite" + "for two" + "King" + "private balcony" + "only 2" all verified in the room JSON. "Playa Hermosa **de Jacó**" STRENGTHENS truth and DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa (which appears in this very SERP) — verified vs `lib/seo.ts` (`addressLocality:"Jacó"`, `streetAddress:"Playa Hermosa, 34"`, Puntarenas, CR). NO invented price, rating, or amenities. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **CORRECTION to the loop brief (important):** The brief stated "Each [room] already emits hotelRoomJsonLd" — this is FALSE. Only **superior** and **estandar** emit `hotelRoomJsonLd`; **junior-suite** and **villas** emit ONLY `breadcrumbJsonLd` (verified by grep across the repo). Wiring `hotelRoomJsonLd` on junior-suite + villas (mirroring `superior/page.tsx` + the Loop 23–24 emission pattern; all fields — name/guests/size/beds — already present in `dict.suites.items`, no price) is the HIGHEST-VALUE next candidate.
- **NEEDS CLIENT INPUT:** None new — starting nightly rate / `priceRange` per room is already logged under "Suites / Rooms hub" in `docs/client-questions-seo.md` (Loops 21/23); guests/size/beds are all present so no new gap. (Date bumped to Loop 26.)
- **Next candidate:** (1) **Wire `hotelRoomJsonLd` on junior-suite + villas** (currently emit only `breadcrumbJsonLd`) — clean verifiable schema add, mirror `superior/page.tsx`. (2) Remaining geo-thin room TITLES: **superior** ("Superior Room | Hotel Terraza del Pacífico"), **estandar**, **villas** — same geo-thin pattern as junior-suite was, apply "in Playa Hermosa de Jacó" + each room's real differentiator (superior = 2 double beds / 4 guests; villas = 6 guests / 72 m²). (3) Restaurant `acceptsReservations: true` schema add (pending since Loop 20).

### Loop 27 (SERP-aware) — 2026-06-25 — junior-suite + villas (hotelRoomJsonLd schema)
- **Schema-emission fix (not a copy change):** Loop 26 found that the junior-suite and villas room detail pages emit **only `breadcrumbJsonLd`** — NO `hotelRoomJsonLd` — while superior and estandar DO. This loop wires `hotelRoomJsonLd` onto **both** pages so every room detail page carries verifiable HotelRoom structured data, fixing the inconsistency. No keyword/SERP target — this is a structured-data parity fix justified by the gap.
- **Change (2 files, mirror `superior/page.tsx` exactly):**
  - `app/[locale]/suites/junior-suite/page.tsx` — added `hotelRoomJsonLd` to the import; in the default Page, look up `room = dict.suites.items.find(r => r.slug === "junior-suite")`, split `dict.suites.amenitiesNote` on "·" into `amenities`, and conditionally emit `<JsonLd data={hotelRoomJsonLd({ locale: l, path, room, description: seo[l].desc, amenities })} />` after the breadcrumb. Description = the page's own localized `seo[l].desc`.
  - `app/[locale]/suites/villas/page.tsx` — same wiring; description = `data.desc[l]` (villas JSON `desc` is a `{es,en}` object, unlike junior-suite's `seo` const).
- **Fields mapped vs omitted (helper omits any undefined, never emits price/rating):**
  - **junior-suite** (`dict.suites.items` slug `junior-suite`): name "Junior Suite", occupancy maxValue **2** (guests "2"), floorSize **32** m² (MTK), bed → BedDetails numberOfBeds **1** / typeOfBed "King" (beds "1 King"), **view "Pool"** (the MAIN POOL — not ocean, per its content), description (localized), amenities (from shared amenitiesNote). Nothing fabricated; no price.
  - **villas** (slug `villas`): name "Villa", occupancy maxValue **6** (guests "6"), floorSize **72** m² (MTK), beds "Multiple" → emitted as the **raw string** (no leading number, so the helper omits a fabricated BedDetails count — correct), **view "Partial ocean"** (matches villas content "Partial ocean view" / "partial ocean views"), description (localized), amenities. No price.
- **Truth check:** Every emitted value read from `dict.suites.items` (guests/size/beds/view) — which matches each room's own JSON content (junior-suite JSON: "2 persons", "32 m²", "1 King Size", "Main pool view"; villas JSON: "6 persons", "72 m²", "Multiple options", "Partial ocean view"). junior-suite view kept as **Pool** (not upgraded to ocean). villas "Multiple" beds emitted as raw string, NOT a fabricated count. No invented price/rating. Geo unchanged. `npx tsc --noEmit` exit 0. Confirmed both new emissions match superior/estandar's pattern (import + conditional `<JsonLd>` JSX).
- **NEEDS CLIENT INPUT:** None new — guests/size/beds/view all present in repo; starting nightly rate / `priceRange` per room already logged under "Suites / Rooms hub" (Loops 21/23/26). (Date bumped to Loop 27.)
- **Next candidate:** (1) Remaining geo-thin room TITLES: **superior**, **estandar**, **villas** — apply "in Playa Hermosa de Jacó" + each room's real differentiator (superior = 2 double beds / 4 guests; villas = 6 guests / 72 m² / partial ocean view). (2) Restaurant `acceptsReservations: true` schema add (pending since Loop 20). (3) Cross-check Menu JSON-LD `isPartOf` ↔ `restaurantJsonLd` `@id`.

### Loop 28 (SERP-aware) — 2026-06-25 — suite detail titles (superior/estandar/villas)
- **Target keyword + SERP (region, US DB):** Shared room/lodging cluster, same anchor as Loops 21/26 — `DATA_getKeywordsMetrics`: **`playa hermosa costa rica hotel` 390 vol / KD 10 (intent I)**; **`hotels playa hermosa costa rica` 140 vol / KD 8 (intent L+C)**; room-type seeds `villa playa hermosa costa rica` + `standard room playa hermosa costa rica` both `is_data_found:false` (room-level queries carry no measured volume, as in every prior room loop). One query was enough — these three pages share intent and the pattern is established (competitor SERP titles room pages "<Room type> - Playa Hermosa hotel <brand>" / "<Room> with Ocean Views"; Guanacaste Playa Hermosa is the in-SERP trap → "de Jacó" disambiguates). This closes the geo-thin-room-TITLE gap flagged in Loops 26–27 (junior-suite was done in Loop 26; superior/estandar/villas were the remaining three).
- **Content gap found:** All three detail `<title>`s were geo-free brand tails — superior "Superior Room | Hotel Terraza del Pacífico", estandar "Standard Room | Hotel Terraza del Pacífico", villas "Villas | Hotel Terraza del Pacífico". Geo ("Playa Hermosa") lived only in each meta DESCRIPTION, which `<title>` outweighs. `seo.title`/`data.title` feeds `pageMetadata` (`<title>`/OG) ONLY — H1 + breadcrumb come from `data.h1[0]` / `dict.suites.items[].name`, and `SuiteDetail` gets only slug/locale/dict — so this is a contained meta-only change (no H1/breadcrumb/CTA side effect, same as Loop 26).
- **Change (3 titles, both locales):**
  - **superior** — `app/[locale]/suites/superior/page.tsx` (local `seo` const). EN `:20` "Superior Room | Hotel Terraza del Pacífico" → "Superior Room with Pool View in Playa Hermosa de Jacó" (54 chars). ES `:16` "Habitación Superior | Hotel Terraza del Pacífico" → "Habitación Superior frente a la piscina en Playa Hermosa de Jacó" (65 chars).
  - **estandar** — `content/habitaciones-estandar.json` (`title` block; fed via `pageMetadata({content: data})`). EN `:5` "Standard Room | Hotel Terraza del Pacífico" → "Standard Room with Garden View in Playa Hermosa de Jacó" (55 chars). ES `:4` "Habitación Estándar | Hotel Terraza del Pacífico" → "Habitación Estándar con vista a los jardines en Playa Hermosa, Jacó" (66 chars).
  - **villas** — `content/habitaciones-villas.json` (`title` block; fed via `pageMetadata({content: data})`). EN `:5` "Villas | Hotel Terraza del Pacífico" → "Villa with Partial Ocean View in Playa Hermosa de Jacó" (54 chars). ES `:4` "Villas | Hotel Terraza del Pacífico" → "Villa con vista parcial al mar en Playa Hermosa de Jacó" (54 chars).
  - All front-load room type + its REAL differentiating view + the full "Playa Hermosa de Jacó" geo; brand tail dropped to fit geo inside budget (EN all 54–55; ES 54–66, the longer two carry the descriptive Spanish view phrase — acceptable for a descriptive geo title). Descriptions left unchanged (each already carries "Playa Hermosa").
- **Truth check (each room's REAL view, verified vs `dict.suites.items` + each room's content — NO ocean upgrades):**
  - **superior = Pool** — `lib/dictionaries.ts` `view:"Pool"`/`"Piscina"`; superior content "Main pool views". Used "Pool View / frente a la piscina". NOT upgraded to ocean.
  - **estandar (Standard Room) = Gardens** — `view:"Gardens"`/`"Jardines"`; estandar JSON desc "garden-facing" / "frente a los jardines" (tropical gardens). Used "Garden View / vista a los jardines". NOT upgraded to ocean.
  - **villas (Villa) = Partial ocean** — `view:"Partial ocean"`/`"Vista parcial al mar"`; villa genuinely has a partial ocean view (matches Loop 27's villas `hotelRoomJsonLd` view), so "Partial Ocean View / vista parcial al mar" is truthful HERE only.
  - "Playa Hermosa de Jacó" STRENGTHENS truth + DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa (which ranks in this very cluster) — verified vs `lib/seo.ts` NAP (`addressLocality:"Jacó"`, `streetAddress:"Playa Hermosa, 34"`, Puntarenas, CR). NO invented price/size/rating. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT:** None new — views/types all present in repo; starting nightly rate / `priceRange` per room already logged under "Suites / Rooms hub" (Loops 21/23/26). (Date bumped to Loop 28.)
- **Next candidate:** (1) **Restaurant `acceptsReservations: true`** schema add to `restaurantJsonLd` (`lib/seo.ts`) — VERIFIABLE from repo ("Walk-ins & reservations" / "Con o sin reserva"; "Dinner reservations recommended"); pending since Loop 20 — the clean schema win. (2) **rooms-compare page** (`/suites/comparar`) — verify its title/meta carry geo + the per-room views. (3) The **Google-Ads feeder-city landing pages** (net-new pages targeting the high-intent commercial clusters surfaced across Loops 20–22).

### Loop 29 — 2026-06-25 — Homepage (`/`) — META TITLE + DESCRIPTION geo-disambiguation
- **Gap found:** The homepage — the highest-value page — was the ONLY geo-bearing page still carrying bare "Playa Hermosa, Costa Rica" in BOTH its `<title>` and meta description, with no "de Jacó" disambiguator. Every room/restaurant/menu page had been getting "Playa Hermosa **de Jacó**" added across Loops 21/25/26/28 to separate the resort from the unrelated **Guanacaste** Playa Hermosa (a confirmed in-SERP competitor trap), yet the homepage — which inherits the most queries — still read ambiguously.
- **Change (1 file — `app/[locale]/page.tsx`, both locales, `homeSeo` const):**
  - TITLE (EN+ES, identical string) — "Hotel Terraza del Pacífico | Playa Hermosa, Costa Rica" → "Hotel Terraza del Pacífico | Playa Hermosa de Jacó, Costa Rica" (61 chars).
  - DESC EN — "Beachfront hotel in Playa Hermosa, Costa Rica. …" → "Beachfront hotel in Playa Hermosa de Jacó, Costa Rica. …" (rest unchanged).
  - DESC ES — "Hotel frente al mar en Playa Hermosa, Costa Rica. …" → "Hotel frente al mar en Playa Hermosa de Jacó, Costa Rica. …" (rest unchanged).
- **Truth check:** "de Jacó" STRENGTHENS truth and DISAMBIGUATES from the unrelated Guanacaste Playa Hermosa — verified vs `lib/seo.ts` NAP (`addressLocality:"Jacó"`, `streetAddress:"Playa Hermosa, 34"`, Puntarenas, CR). No amenity/rating/price change; all other facts in title+desc untouched. `seo.title/desc` feed `pageMetadata` (`<title>`/`description`/OG) only — H1 comes from `data.h1[0]`, so no on-page side effect. Typecheck clean (`npx tsc --noEmit`, exit 0).
- **NEEDS CLIENT INPUT:** None new.
- **Next candidate:** (1) Restaurant `acceptsReservations: true` schema add to `restaurantJsonLd` (`lib/seo.ts`) — pending since Loop 20. (2) rooms-compare page (`/suites/comparar`) title/meta geo check. (3) Google-Ads feeder-city landing pages.

#### Loop 29 addendum — About page (`/about`) + brand-SERP validation
- **Live data (US DB, `DATA_getKeywordsMetrics`):** Demand is overwhelmingly BRANDED — **`terraza del pacifico` 260 vol / KD 6 (intent L+N)**, `hotel terraza del pacifico` 90 vol / KD 6 (L+C); generic geo seeds (`hotel playa hermosa jaco costa rica`, `hotels near jaco costa rica beach`) `is_data_found:false`; `playa hermosa jaco costa rica hotels` + `playa hermosa de jaco` only 10 vol each. Brand-first titles on both pages are correct.
- **Live SERP (`terraza del pacifico costa rica`, US):** `terrazadelpacifico.com/` ranks **#1 organic** w/ sitelinks (→ /restaurante, /habitaciones); rest of page = branded OTA wall (TripAdvisor, Booking, Expedia, Agoda, Trivago, hoteles.com…). **Nearly every OTA listing ties the hotel to "Jacó"** (TripAdvisor URL `…Playa_Hermosa_Jaco_District…`, FB "…| Jacó", Expedia "Jaco-Hotels…", hoteles.com "…en Jacó", Agoda "…in Jaco", Trivago "…-jacó"). Our homepage was the ONLY property in its own brand SERP saying bare "Playa Hermosa" → Loop 29's "de Jacó" title/desc edit is **directly validated by live data** (aligns us with how Google + the ecosystem already file this hotel).
- **About change:** Title left brand-first ("About us | Hotel Terraza del Pacífico") — About pages don't surface for brand head terms and there's no "about"-geo demand, so geo in the title is low-value. Applied the same disambiguation to the DESCRIPTION only (both locales): "…en/at Playa Hermosa" → "…en/at Playa Hermosa **de Jacó**". Truth-verified vs `lib/seo.ts` NAP; no other facts touched. Typecheck clean.

### Loop 30 — 2026-06-25 — Child-slug migration (Pass 2): `estandar→standard`, `comparar→compare`
- **Context:** Pre-launch (site goes live tomorrow). Investigated the "remaining Spanish child slugs" flagged in memory `tdp-english-first-slugs` Pass 2 ("bodas→weddings, otros→spaces, estandar→standard, comparar→compare"). **Finding: events were NEVER broken** — `app/[locale]/events/[slug]` routes via per-locale slugs (`en: weddings/other-events/surf-nights`, `es: bodas/otros`), so English visitors already get English event URLs. The `events/{bodas,otros,surf-nights}` dirs are empty + git-untracked cruft. The ONLY real problem was the two SUITE detail folders (static folder = one Spanish slug shared by both locales).
- **`comparar→compare` (contained, not a data key):** `git mv` folder; updated `path` const, `site-header.tsx` dark-hero check + comment, `suites-hub.tsx` ×2 hrefs, `suites-hub.ts` ×2 hrefs, `lib/seo.ts` sitemap route. No body-copy "comparar" exists, so URL-token replace was safe.
- **`estandar→standard` (atomic — `estandar` doubles as the room identifier/lookup key):** `git mv` folder; `page.tsx` path+slug consts (kept the `habitaciones-estandar.json` import name); whole-word `\bestandar\b`→`standard` across the 7 files that key on it — `lib/dictionaries.ts` (items[].slug ×2 locales), `suites-hub.ts` (hubRoomOrder + copy.rooms ×2), `suite-detail.ts` (×2), `rooms-compare.ts` (roomFacets), `home/suites.tsx` (ROOM_EXTRA_IMAGES), `room-galleries.ts` (slug), `lib/seo.ts` (sitemap). Hub/detail/home links are built as `/suites/${room.slug}`, so they followed automatically.
- **Redirects:** added child redirects `/:locale/suites/estandar→standard` and `comparar→compare` in `next.config.mjs` (catch old bookmarks + the 2nd hop of old-site `/habitaciones/estandar`).
- **Vestigial (left as-is):** `content/habitaciones-estandar.json` `url`/`slug` fields — `pageMetadata` builds canonical/OG from the `path` arg (`lib/seo.ts:88`), not from `content.url`; confirmed. `content/_crawler.js` = archival old-site crawl map.
- **Verification (tsc is BLIND to these stringly-typed lookups — verified by rendering instead):** `npm run build` green; new routes `/{es,en}/suites/{standard,compare}` generated, no estandar/comparar routes. Grepped prerendered HTML: standard detail headline ("garden-facing room for up to four" / "serena frente al jardín") renders EN+ES; suites hub renders the Standard card (kicker "Quiet & serene") and links to `/suites/standard`; compare page links to `/suites/standard`; homepage carousel references `suites/standard`. No silent missing-data card on any of the 4 surfaces. No button points at a dead slug.
- **Pass 2 = COMPLETE** (events were a false alarm). Memory `tdp-english-first-slugs` updated to reflect this.
